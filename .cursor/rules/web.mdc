---
description: Seogadam 웹 앱 개발 규칙 (apps/web)
globs:
  - apps/web/**
alwaysApply: false
---

# Seogadam Web 개발 규칙

모든 저장소 변경은 먼저 `docs/todo/` 일지·승인·`@history` 규칙을 따른다 (`.cursor/rules/workflow-todo-approval.mdc`).

## 서비스 개요

Bookfolio는 **개인 소장 도서 관리** 서비스이다.
웹과 모바일에서 내가 소장하고 있는 책들을 언제 어디서든 확인할 수 있고,
책을 구매할 때 이미 소장 중인지 ISBN 입력 또는 바코드 스캔으로 검색할 수 있다.

### 핵심 기능

1. **소장 관리** — 도서 등록·편집·삭제, 읽기 상태, 별점, 메모
2. **검색** — ISBN 입력 / 바코드 스캔으로 소장 여부 확인 및 도서 정보 조회

## 기술 스택

| 영역          | 기술                                                                           |
| ------------- | ------------------------------------------------------------------------------ |
| 프레임워크    | Next.js 15 (App Router)                                                        |
| 언어          | TypeScript (strict)                                                            |
| UI 라이브러리 | **shadcn/ui** (Radix UI + Tailwind CSS)                                        |
| 스타일링      | Tailwind CSS v4                                                                |
| 폰트          | **Pretendard** (로컬 셀프호스팅, `apps/web/public/fonts/`)                     |
| 인증          | **NextAuth.js (Auth.js v5)** — 이메일·비밀번호(Credentials) + **Google OAuth** |
| DB            | Supabase (PostgreSQL), `user_books` 등은 **서비스 롤 + 앱에서 user id 검증**   |
| 패키지 매니저 | pnpm (모노레포 workspace)                                                      |

## 인증 (NextAuth)

- 설정: `src/auth.config.ts` (프로바이더·JWT 콜백), `src/auth.ts` (handlers / `auth` / `signIn` / `signOut`), API `app/api/auth/[...nextauth]/route.ts`
- 미들웨어는 Edge 호환을 위해 `src/auth.edge.ts` 최소 설정만 사용 (JWT 검증, `AUTH_SECRET` 동일)
- 이메일 가입: `POST /api/auth/register` → `app_users` 테이블에 bcrypt 해시 저장
- Google: 동일 이메일이면 기존 `app_users` 행과 매칭 (`lib/auth/app-users.ts`의 `ensureOAuthAppUser`)
- 로그아웃: `POST /auth/signout` → `signOut({ redirectTo: "/login" })`
- 필수 환경 변수: `AUTH_SECRET` (또는 호환용 `NEXTAUTH_SECRET`), Google 사용 시 `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`
- DB 마이그레이션: `supabase/migrations/0002_app_users_nextauth.sql` 적용 후 기존 `profiles` id가 `app_users`로 복사되고 `user_books.user_id` FK가 `app_users`를 가리킴
- API `Authorization: Bearer` + Supabase JWT는 모바일 등 레거시 호환용으로 유지 가능 (`getRequestUserId`)

## 폰트 설정

- Pretendard woff2/woff 파일은 `apps/web/public/fonts/` 에 위치 (Next.js 정적 제공 규약)
- `@font-face`로 로컬 셀프호스팅 (CDN 사용 금지)
- 사용 가능한 웨이트: Thin(100), ExtraLight(200), Light(300), Regular(400), Medium(500), SemiBold(600), Bold(700), ExtraBold(800), Black(900)
- woff2를 우선 로드하고 woff를 폴백으로 지정
- CSS 변수 또는 Tailwind 설정에서 `--font-sans: "Pretendard", system-ui, sans-serif`로 지정
- body 기본 폰트: Pretendard Regular(400), 본문 기본 사이즈 16px

## shadcn/ui 규칙

- 컴포넌트 위치: `apps/web/src/components/ui/`
- shadcn/ui CLI로 추가한 컴포넌트만 사용하고, 직접 Radix를 import하지 않는다
- 커스텀 컴포넌트는 `apps/web/src/components/` 하위에 도메인별로 구성
  - `components/books/` — 도서 관련 UI
  - `components/layout/` — 레이아웃 (Header, Sidebar, Shell 등)
  - `components/search/` — 검색·ISBN 조회 관련 UI
- Tailwind 유틸리티 클래스 사용 (레이아웃·간격·타이포), 인라인 스타일은 사용하지 않는다
- `cn()` 유틸리티 (`lib/utils.ts`)로 조건부 클래스 병합

## 디렉터리 구조

```
apps/web/src/
├── app/                    # Next.js App Router 페이지
│   ├── (auth)/             # 인증 관련 (로그인 등)
│   ├── (main)/             # 인증 후 메인 레이아웃
│   │   ├── dashboard/      # 대시보드 (내 서가)
│   │   └── books/          # 도서 상세·편집
│   ├── api/                # API Route Handlers
│   ├── layout.tsx
│   └── page.tsx            # 랜딩 페이지
├── components/
│   ├── ui/                 # shadcn/ui 컴포넌트
│   ├── books/              # 도서 도메인 컴포넌트
│   ├── layout/             # 레이아웃 컴포넌트
│   └── search/             # 검색 관련 컴포넌트
├── lib/
│   ├── supabase/           # Supabase 클라이언트
│   ├── books/              # 도서 비즈니스 로직
│   ├── auth/               # 인증 헬퍼
│   ├── env.ts              # 환경 변수
│   └── utils.ts            # cn() 등 유틸리티
├── hooks/                  # 커스텀 React 훅
├── auth.config.ts          # NextAuth 설정 (Node)
├── auth.ts                 # NextAuth 인스턴스
└── auth.edge.ts            # 미들웨어용 최소 NextAuth 설정

apps/web/public/
├── assets/                 # 정적 이미지(SVG·PNG 등). Stitch 시안 이미지도 여기에 받아 둔다
└── fonts/                  # Pretendard (woff2 / woff)

### 선반·장식 소품 이미지(스프라이트)

- 여러 소품을 **한 장(맵/아틀라스)**에 묶어 두었다면 **CSS 스프라이트**로 잘라 쓸 수 있다: 고정 크기 래퍼 + `background-image: url(/assets/...)` + `background-size`(전체 시트 기준) + `background-position`(타일 좌표). 또는 코드 상수 **맵**(키 → `{ x, y, w, h }` 비율)으로 위치만 관리하고 동일 방식으로 적용한다.
- 구현 참고: `lib/dashboard/shelf-prop-sprites.ts`, `components/dashboard/shelf-prop-sprite.tsx`. 시트 파일 `public/assets/shelf-props.png`를 둔 뒤 맵 좌표를 맞춘다. 대시보드 표지 행 양쪽 장식은 **`NEXT_PUBLIC_SHELF_PROPS_SPRITE_ENABLED=1`** 일 때만 노출되도록 할 수 있다(미설정·`0`이면 비표시).
- 장식은 `aria-hidden`·`pointer-events-none` 등으로 **접근성·클릭 충돌**을 피한다.
    ├── woff2/
    └── woff/
```

## Stitch 디자인 반영 (MCP·시안 이식)

- 레이아웃·카피·컴포넌트 구조는 Stitch HTML을 **참고**해 TSX + Tailwind + shadcn으로 옮긴다 (인라인 스타일·원본 HTML 그대로 붙여넣기 지양은 기존 스타일 규칙과 동일).
- **이미지(필수)**  
  - Stitch 산출물에 포함된 **`lh3.googleusercontent.com` 등 외부 호스트 URL을 최종 코드에 직접 두지 않는다.** (만료·접근 제한·추적 이슈, 폰트 CDN과 마찬가지로 배포를 외부에 묶지 않음.)
  - 시안에 쓰인 원본 URL에서 파일을 받아 **`apps/web/public/assets/`** 에 두고, 파일명은 용도가 드러나게 한다 (예: `landing-hero-bg.png`, `stitch-login-dark-panel-bg.png`, `stitch-login-dark-brand-logo.png`).
  - 코드에서는 **`/assets/...` 로컬 경로**만 참조한다 (`next/image` 포함).
  - **`next.config.mjs`의 `images.remotePatterns`를 Stitch 이미지용으로 상시 추가하지 않는다.** 임시로 원격을 켠 경우, 자산을 `public/assets`로 옮긴 뒤 패턴을 제거한다.
- 요약 절차: **다운로드 → `public/assets` 저장 → 코드는 로컬 경로만.**

## 코딩 컨벤션

### TypeScript

- `strict: true` 유지
- `any` 사용 금지, 명시적 타입 선언
- 경로 별칭 `@/*` → `./src/*` 사용
- 서버/클라이언트 컴포넌트 구분 명확히 (`"use client"` 최소 범위)

### 컴포넌트

- 서버 컴포넌트를 기본으로 하고, 상태/이벤트가 필요한 경우에만 클라이언트 컴포넌트
- Props 타입은 컴포넌트 파일 내에 `interface`로 정의
- 재사용 컴포넌트는 named export, 페이지는 default export

### API Route

- `app/api/` 하위에 RESTful 구조
- 에러 응답은 `{ error: string }` 형태로 통일
- 인증이 필요한 API는 `request-user.ts` 헬퍼로 세션 확인

#### HTTP 메서드 (필수)

- **애플리케이션이 정의·호출하는 메서드는 `GET`과 `POST`만 허용한다.**
- **`PUT`, `PATCH`, `DELETE`, `HEAD`, `TRACE` 등 그 외 메서드는 사용하지 않는다.**
  - Route Handler에서 `export async function PATCH` / `DELETE` 등을 두지 않는다.
  - 클라이언트(웹·Flutter 등)에서도 `method: "PATCH"` 등으로 호출하지 않는다.
- **`OPTIONS`는 “우리가 API 설계로 쓰는 메서드”가 아니다.** 브라우저 CORS 프리플라이트 등으로 인해 서버 로그에 `OPTIONS … 204`가 찍힐 수 있다. 이는 Next.js·런타임이 처리하는 경우가 많으며, **의도적으로 `OPTIONS` 핸들러를 추가하거나, 클라이언트에서 `OPTIONS`를 호출하는 패턴은 쓰지 않는다.**
- 수정·삭제 의미는 `POST`로 표현한다. 예: 동일 경로에 `POST` + JSON 본문의 `action: "update" | "delete"` 또는 전용 하위 경로 `POST .../update`, `POST .../delete` 등 팀에서 정한 하나의 패턴으로 통일.
- 조회·멱등 읽기만 `GET`으로 둔다.

### 스타일

- Tailwind 유틸리티 클래스만 사용 (globals.css에 커스텀 CSS 최소화)
- 반응형 디자인: Tailwind는 `sm:`·`md:`·`lg:` 순서로 작성한다.
- **웹(apps/web) 목표 뷰포트:** 레이아웃·간격·시각 검증은 **데스크톱과 최소 태블릿**(대략 `md` 이상, 약 768px 이상)을 기준으로 한다. 폰 단독 웹 퍼스트 레이아웃까지 맞추는 작업은 우선순위에서 제외해도 된다 — **모바일은 별도 Flutter 앱**이 담당한다.
- 다크 모드: `globals.css`에 `.dark` 토큰 정의됨, 필요 시 루트에 클래스 토글로 활성화
- 색상은 shadcn/ui CSS 변수 시스템 활용

### React 하이드레이션 (Next.js App Router)

- 개발 중 콘솔에 **“A tree hydrated but some attributes of the server rendered HTML didn't match”**가 뜨면, 먼저 **앱 코드**에서 `Date.now()`·`Math.random()`·`typeof window` 분기·잘못된 HTML 중첩·서버/클라이언트 **로케일·타임존**만 다른 포맷 등을 의심한다.
- 그런데 diff에 **`data-sharkid`**처럼 **서버 HTML에는 없고 클라이언트에만 생기는 속성**이면, 브라우저 **확장 프로그램**(자동완성·비밀번호·보안 등)이 하이드레이션 전에 DOM을 수정한 경우가 많다. 앱 버그가 아닐 수 있다.
- 재현을 줄이려면: 확장 끄기·시크릿 창·또는 해당 폼 컨트롤에 **`suppressHydrationWarning`**(해당 노드에서 **속성** 불일치 경고만 무시)을 쓸 수 있다. **남용하지 말고**, 네이티브 `<input>` / `<select>` 등 확장이 자주 건드리는 노드에만 한정한다. 근본 원인이 로케일/시간/분기 코드면 그쪽을 고친다.

## 핵심 도메인 모델 참조

```typescript
interface Book {
  id: string;
  userId: string;
  isbn: string;
  title: string;
  author: string;
  publisher?: string;
  coverUrl?: string;
  status: "unread" | "reading" | "finished";
  rating?: number; // 1-5
  memo?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 주의사항

- ISBN 조회 API는 `lib/books/lookup.ts`에 추상화되어 있으며, 제공자 변경에 대비
- Supabase: 서버 `server.ts`(서비스 롤로 도서 CRUD), 브라우저 `browser.ts`는 레거시·확장용
- 환경 변수는 `lib/env.ts`에서 검증 후 사용
- `@bookfolio/shared` 패키지의 공통 타입·상수 우선 사용
