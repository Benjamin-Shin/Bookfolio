# 2026-06-05

## 모바일 JWT sliding refresh + 401 처리

- **범위:** `apps/web/src/lib/auth/mobile-jwt.ts`, `apps/web/src/app/api/auth/mobile/refresh/route.ts`, `apps/mobile/lib/src/util/jwt_claims.dart`, `auth_controller.dart`, `auth/auth_gate.dart`
- **할 일:**
  - `POST /api/auth/mobile/refresh` — 유효·만료 7일 grace 토큰으로 새 30일 JWT 발급, `iat` 기준 90일 absolute max
  - 클라이언트: `exp` 7일 미만·grace 만료 토큰 갱신, resume 6h debounce, restore/resume/401 연동
  - 401 시 refresh 1회 후 실패하면 `signOut` + 로그인 화면; `AuthGate` `isRestoring` 대기
- **상태:** `done`
