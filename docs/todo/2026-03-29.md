# 2026-03-29

**직접 지시(채팅):** 모바일 — 공동서재·내서재 **책 정보 조회(상세) 화면** 다크 테마 처리 (`shared_library_book_detail_screen.dart`, `book_detail_screen.dart`). **상태:** `done`.

---

| 항목 | 내용 |
| --- | --- |
| **제목** | 개인정보처리방침 페이지 보완 (트레바리 문 참고) |
| **범위** | `apps/web/src/app/privacy/page.tsx` |
| **할 일** | 도입부·필수·선택 구분, 자동 수집·수집 방법 보강, 법정 보존·파기 절차, 제3자 제공 예외(익명 통계·급박 위험), 쿠키 설정 안내, 안전성 조치 구분, 적용 범위(외부 링크), 개정 공지(7일·30일), 피해 구제 기관, 향후 결제 시 PG 수집 안내 |
| **상태** | `approved` (채팅에서 「적용하자」로 구현 지시 — 승인으로 간주) |

| **제목** | 개인정보처리방침 — 캐논·투표 용어를 이용자 친화적으로 정의 |
| **범위** | `apps/web/src/app/privacy/page.tsx` |
| **할 일** | 캐논·투표 수집 항목 및 관련 문장에 쉬운 설명 병기 |
| **상태** | `done` |

| **제목** | 서비스 이용약관 보완 (트레바리 등 참고 조항 반영) |
| **범위** | `apps/web/src/app/terms/page.tsx` |
| **할 일** | 공고·시행일, 용어(이용자·게시물 등), 약관 개정(7일·30일), 개별약관 우선, 이용계약·회원정보 변경, 통지, 운영자 의무, 서비스 제공·변경, 정보·광고, 게시물 관리·권리 귀속, 환불·이용제한, 분쟁·강행법규 등 Bookfolio에 맞게 반영 |
| **상태** | `done` |

| **제목** | 모바일(웹·앱)에서 개인정보·약관·쿠키 접근 |
| **범위** | `apps/web/src/components/layout/site-header*.tsx`, `apps/mobile` 드로어·`bookfolio_web_urls.dart` |
| **할 일** | `md` 미만 웹 Sheet 햄버거에 법적 고지 3링크·내비 수집; Flutter 햄버거 드로어에 동일(외부 브라우저·`BOOKFOLIO_API_BASE_URL` origin) |
| **상태** | `done` (채팅 직접 지시) |

| **제목** | 랜딩 홈 — Bookfolio 영문 표기·서비스 소개 패널 |
| **범위** | `apps/web/src/app/page.tsx` |
| **할 일** | 상단 소개 패널(Book+Portfolio 의미, 소장·공유); 3칸 미니 카드 제거; 본문 내 한글 상표 「북폴리오」→ `Bookfolio` |
| **상태** | `done` |

| **제목** | 관리 화면 — 공동서재 목록(멤버·도서 수) |
| **범위** | `apps/web/src/app/dashboard/admin/shared-libraries/`, `layout.tsx`, `admin/page.tsx`, `lib/libraries/admin-shared-libraries-overview.ts` |
| **할 일** | 전체 공동서재 테이블: 이름·유형·멤버 표시·연결된 캐논 도서 수(중복 제거) |
| **상태** | `done` |

| **제목** | 모바일 — 도서 추가(등록) 화면 UI 정리 |
| **범위** | `apps/mobile/lib/src/ui/screens/book_form_screen.dart` |
| **할 일** | 표지 프리뷰·섹션 카드·빠른 입력(바코드·ISBN·제목)·별점 등 레이아웃 |
| **상태** | `done` (채팅 직접 지시) |

| **제목** | 모바일 — 내서재 도서 상세 화면 섹션(카드) 레이아웃 |
| **범위** | `apps/mobile/lib/src/ui/screens/book_detail_screen.dart` |
| **할 일** | 읽기 상태·독서 이벤트·도서 정보·한줄평·메모·타임라인을 구역 카드로 구분 |
| **상태** | `done` (채팅 직접 지시) |

| **제목** | 모바일 프로필 — 회원 탈퇴(웹과 동일 `DELETE /api/me/account`) |
| **범위** | `apps/mobile/lib/src/services/bookfolio_api.dart`, `profile_screen.dart` |
| **할 일** | 탈퇴 확인 다이얼로그·API 호출 후 로그아웃 |
| **상태** | `done` |

| **제목** | 모바일 내 서재 — 웹과 동일 소장 책 가격 합계 표시 |
| **범위** | `apps/web/src/app/api/me/stats/owned-books-price/route.ts`, `apps/mobile` API·컨트롤러·`library_screen.dart` |
| **할 일** | `GET /api/me/stats/owned-books-price` + Flutter 조회·상단 배너 |
| **상태** | `done` (채팅에서 직접 구현 지시) |

| **제목** | 랜딩(루트) — 로그인 시 「로그인 · 가입」 버튼 숨김 |
| **범위** | `apps/web/src/app/page.tsx` |
| **할 일** | `auth()`로 세션 확인 후 비로그인일 때만 `/login` 링크 버튼 표시 |
| **상태** | `done` |

| **제목** | 웹 — 모바일 화면 시뮬레이션(기기 프레임·iframe) 페이지 + 루트 연결 |
| **범위** | `apps/web/src/app/mobile-preview/`, `apps/web/src/app/page.tsx` |
| **할 일** | `/mobile-preview`에 휴대폰 베젤·뷰포트로 사이트 로드; 랜딩 CTA에서 링크 |
| **상태** | `done` |

| **제목** | 모바일 웹 미리보기 — 햄버거 동일 경로·상세(ID)·법적 고지 프리셋 |
| **범위** | `apps/web/src/app/mobile-preview/mobile-device-preview.client.tsx`, `page.tsx` |
| **할 일** | 공동서재·집계·베스트셀러·초이스·내 서재·약관·쿠키; 내/공동 상세 UUID 입력 |
| **상태** | `done` (채팅 직접 지시) |

| **제목** | 랜딩 — 모바일 웹 미리보기(`/mobile-preview`) CTA 제거 |
| **범위** | `apps/web/src/app/page.tsx` |
| **할 일** | iframe 미리보기는 별도 URL만 유지; 홈 히어로에서 링크 제거 |
| **상태** | `done` (채팅 직접 지시) |

| **제목** | 관리 홈 상단 — 플랫폼 통계 지표 |
| **범위** | `apps/web/src/app/dashboard/admin/page.tsx`, `apps/web/src/lib/admin/admin-dashboard-stats.ts` |
| **할 일** | 사용자 수(`app_users`), 공동서재 수(`libraries`), 캐논 도서(`books`) 및 소장 권수·저자·유효 VIP 등 집계 카드 |
| **상태** | `done` (채팅 직접 지시) |
