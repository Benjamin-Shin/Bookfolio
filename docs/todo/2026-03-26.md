# 2026-03-26

## 대시보드 내 서재 UI

- **범위:** `apps/web/src/app/dashboard/page.tsx`, 대시보드 툴바·장르 필터
- **할 일:** 왼쪽 메뉴 카드 제거, 읽기 상태·소장 탭(`tab` 쿼리), 상단 통계 지표 카드
- **상태:** done

## 대시보드 독서 이벤트 캘린더

- **범위:** `apps/web/src/app/dashboard/page.tsx`, `components/dashboard/dashboard-reading-events-calendar.client.tsx`
- **할 일:** 모바일 `MyStatsScreen`과 같이 월별 독서 이벤트 집계 캘린더 표시
- **상태:** done

## 캘린더 일별 이벤트 테이블·표지

- **범위:** `user-book-sidecars.ts`, `api/me/reading-events/by-day`, `dashboard-reading-events-calendar.client.tsx`
- **할 일:** 이벤트가 있는 날 선택 시 하단 테이블 + 도서 표지
- **상태:** done

## 모바일 책 상세 — 메모용 글귀 OCR

- **범위:** `apps/mobile` (`pubspec.yaml`, Android/iOS 권한, `book_detail_screen.dart`, OCR 유틸)
- **할 일:** 책 상세 메모 입력란에서 촬영 → OCR 텍스트를 필드에 채움 → 사용자가 편집 후 저장
- **상태:** done

---

## 포인트·캐논 기여·STAFF (플랜 기반 구현 묶음)

기획 출처: Cursor 플랜 `포인트·기여_정책_플랜_05276d73` (캐논 수정요청, 원장·정책 버전, 투표·월간 순위, `books.format` / `user_books.format`, `STAFF`). 채팅 「진행해줘」로 착수·일부 반영(2026-03-26).

### DB 마이그레이션 — 서지·역할 (`books.format`, `STAFF`)

- **범위:** `supabase/migrations/`, [docs/bookfolio-db-schema.md](../bookfolio-db-schema.md)
- **할 일:** `books.format` 추가(NOT NULL, CHECK `paper|ebook|audiobook|unknown`, 기본·백필 `paper`). `user_books.format` 제거, `(user_id, format)` 등 인덱스 정리. `app_users.role` CHECK에 `STAFF` 추가. **동일 PR/커밋 묶음**에서 스키마 문서 반영.
- **상태:** done

### DB 마이그레이션 — 포인트·수정요청·투표

- **범위:** `supabase/migrations/`, [docs/bookfolio-db-schema.md](../bookfolio-db-schema.md)
- **할 일:** `point_rule`, `point_rule_version`, `point_ledger`(멱등 키 UNIQUE), 선택 `user_point_balance`. `book_canon_edit_requests`(또는 확정 테이블명): book_id, submitter_id, field(page_count|genre_slugs|cover_url), payload, status, reviewed_by/at, rejection_reason. `contribution_votes`: request_id, voter_id, vote, UNIQUE(voter, request). RLS는 서버 전용 패턴. 문서 동기화.
- **상태:** done — `0021`에 `point_rule_versions`/`point_rules`/`user_points_ledger`/`book_canon_edit_requests`/`canon_vote_*`/`monthly_contribution_rankings` 스캐폴드·시드(점수 0) 반영. 필드 스키마는 플랜대로 추가 조정 가능.

### RPC·리포지토리 — `list_user_books_paged` 및 format 필터

- **범위:** `supabase/migrations/`(함수 재정의), `apps/web`·`apps/mobile`에서 해당 RPC/조회 호출부
- **할 일:** `p_format` 조건을 `ub.format` → `b.format`으로 변경. 응답 JSON에 `books.format` 포함 여부 확인.
- **상태:** done

### 웹 — 역할 `STAFF` (세션·어드민)

- **범위:** NextAuth 타입/콜백, `apps/web` 권한 가드, `dashboard/admin/users` 등
- **할 일:** `UserRole`에 `STAFF`. 감수 API/UI는 `ADMIN | STAFF`. `ADMIN`만 타 계정에 `STAFF` 부여 가능하도록 UI·서버 검증.
- **상태:** draft — DB·세션·`dashboard/admin/users` 역할 선택까지 반영. 감수 API/UI·STAFF 전용 라우트는 미구현.

### 웹 — 포인트 조회·원장 API

- **범위:** `apps/web/src/app/api/` 또는 repository
- **할 일:** 본인 잔액(ledger 합 또는 balance 캐시)·내역 페이지네이션 API. 서비스 롤/세션 검증. (화면은 아래 3항)
- **상태:** draft

### 웹 — 헤더(계정 메뉴) 포인트 잔액

- **범위:** `apps/web/src/components/layout/header-account.tsx`(또는 동등), 포인트 API 연동
- **할 일:** 로그인 시 잔액 표시(숫자 또는 짧은 라벨), 내역 페이지 링크. 로딩·미로그인 시 생략/스켈레톤.
- **상태:** draft

### 웹 — 대시보드 포인트 카드

- **범위:** `apps/web/src/app/dashboard/`(예: 메인 대시보드)
- **할 일:** 잔액 요약 카드, (선택) 최근 적립/차감 1~3건 프리뷰 또는 내역으로 이동 CTA.
- **상태:** draft

### 웹 — 포인트 내역 페이지

- **범위:** `apps/web/src/app/dashboard/…`(신규 라우트, 경로 확정)
- **할 일:** 원장 테이블/목록(일시·증감·사유/규칙 요약·참조), 페이지네이션, 헤더/대시보드와 링크 연결.
- **상태:** draft

### 웹 — 정책 시드 또는 관리

- **범위:** 마이그레이션 시드 또는 어드민 전용
- **할 일:** `point_rule` + 초기 `point_rule_version.params_json`(승인 필드별 점수, 투표 보상 on/off, 일 5회, 월간 구간표·순위식 계수 등 최소값).
- **상태:** draft

### 웹 — 캐논 수정요청 제출·내역

- **범위:** `apps/web` 사용자 폼·API
- **할 일:** 대상 book·필드 1건당 요청 생성(`pending`). 본인 요청 목록 조회. `books` 직접 PATCH 금지.
- **상태:** draft

### 웹 — 감수 큐·승인·거절(트랜잭션)

- **범위:** `apps/web` 관리자/STAFF UI, 서버 액션 또는 API
- **할 일:** 동일 book·필드 `pending` 그룹 표시, 유사 시 선등록 가이드. 승인 시 단일 트랜잭션: `FOR UPDATE` 요청 → `books` 반영 → status + reviewed_by/at → `point_ledger` 적립(멱등 키). 거절 시 reviewed_by/at + rejection_reason. 표지는 감수자 육안 판단.
- **상태:** draft

### 웹 — 기여 투표·일일 한도

- **범위:** API + UI(수정요청 상세)
- **할 일:** 추천/비추, `(request, voter)` 유니크, 유저당 일 5회(합산). 비추는 제출자 원장 미변경. 투표자 소액 적립은 `params_json` on/off.
- **상태:** draft

### 웹·배치 — 월간 순위 적립

- **범위:** Vercel cron / Supabase Edge / 수동 스크립트 등 (인프라 결정)
- **할 일:** 확정 순위식(승인 가중 + upvote×가중 − downvote 페널티)으로 전월 집계, 구간별 지급, `idempotency_key`로 월별 중복 방지.
- **상태:** draft

### 웹 — 소장 등록 시 메타 보완 포인트

- **범위:** `user_books` 생성/연결 API·폼
- **할 일:** `OWNED_BOOK_REGISTER_METADATA` 규칙: 트리거 필드·멱등·일/계정 상한, 상시 지급. ledger 기록.
- **상태:** draft

### 웹 — 포인트 소비(SKU) 최소치

- **범위:** 정책 + API + UI 일부
- **할 일:** 기능/코스메틱 SKU 예시 1~2개, 차감 ledger, 잔액 부족 처리.
- **상태:** draft

### 모바일 — `format` 이전

- **범위:** `apps/mobile` (모델, API 파싱, 도서 추가/목록)
- **할 일:** 소장·책 메타에 `user_books.format` 제거, `books.format` 표시/필터 API 스펙에 맞춤.
- **상태:** done — `BookFormat` 4종·API 파싱 방어; 목록/추가는 서버가 `format`을 캐논에서 내려줌.

### 통합·검증

- **범위:** 전체
- **할 일:** E2E 또는 수동 시나리오: 부록 G 마이그레이션 후 목록 필터, STAFF 감수 한 사이클, 포인트 적립·투표 한도, 문서-DB 일치 확인.
- **상태:** draft

---

## 회원 탈퇴 · 공동서재 소유권

- **범위:** `delete-app-user.ts`, `api/me/libraries/.../transfer-ownership`, `library-members-panel`, `header-account`
- **할 일:** 타 멤버가 있는 소유 공동서재는 탈퇴 전 소유권 이전 안내·API·UI; 본인만 남은 서재는 `app_users` 삭제 시 `libraries.created_by` CASCADE로 정리(별도 수동 삭제 불필요).
- **상태:** done — 채팅 요구사항 반영
