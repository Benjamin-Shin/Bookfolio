# 2026-05-24

## 발견 도서 상세 하단 잘림 보정

- **범위:** `discovery_book_detail_screen.dart`, `mobile_scroll_padding.dart`
- **할 일:** 쉘 하단 5탭·`extendBody` 겹침 — `bookfolioShellBottomNavInset` 기반 ListView 하단 패딩
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 내 서가 도서 상세 — 한줄평 작성

- **범위:** `apps/mobile/lib/src/ui/screens/library/book_detail_screen.dart`
- **할 일:** 캐논 공개 한줄평 입력·저장·삭제(500자), 다른 회원 한줄평 목록 표시
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 모바일 표지 Image.network errorBuilder

- **범위:** `apps/mobile` 표지 `Image.network` (errorBuilder 없는 곳)
- **할 일:** 로드 실패 시 placeholder — FlutterError·오류 리포트 다이얼로그 방지
- **상태:** `done` (채팅 "1번만" 지시)

## 대시보드 히어로 명언 — KST 하루 고정

- **범위:** `dashboard-home-hero.tsx`, `lib/dashboard/home-hero-quotes.ts`
- **할 일:** 독서·서가 톤 문장 풀(검색·재구성), KST 날짜 시드로 1문장·출처 표시
- **상태:** `done` (채팅 2번·문장 검색 지시)

## user_books 태그 — 웹·모바일(Flutter 웹 포함)

- **범위:** `supabase/migrations/0046_*`, `packages/shared`, `apps/web`(API·대시보드·책 등록/상세), `apps/mobile`(모델·API·내 서가·책 폼), `docs/bookfolio-db-schema.md`
- **할 일:** `user_books.tags`(최대 5)·목록 `tag` 필터·태그 칩 UI; 장르(`books.genre_slugs`)는 통계용 유지
- **상태:** `done` (채팅에서 모바일·웹 동시 구현 지시)

## 모바일 저장·수정 연타 방지

- **범위:** `mutation_guard.dart`, `bookfolio_api.dart`, `library_controller.dart`, `book_detail_screen.dart`, `book_form_screen.dart`
- **할 일:** 변경 API coalesce 미들웨어 + UI `AsyncActionGate`로 스낵바·목록 재로드 중복 방지
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 발견 — 나에게 맞는 책 추천 더보기 제거

- **범위:** `apps/mobile/lib/src/ui/screens/discovery/discover_screen.dart`
- **할 일:** 전용 화면 없는 스텁 더보기 버튼 제거
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 발견 — 카테고리 캡션 CID 제거

- **범위:** `discover_screen.dart` (`_displayLabelForAladinCategory`, `_appliedCategoriesCaption`)
- **할 일:** 「기본 카테고리 적용」·적용 분야 목록에 장르 이름만 표시
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 발견 — 베스트·초이스 전체 화면 카테고리 CID 제거

- **범위:** `aladin_category_display.dart`, `bestseller_screen.dart`, `choice_new_screen.dart`
- **할 일:** 목록 상단 안내 문구에 장르 이름만 표시(공통 util)
- **상태:** `done` (채팅 승인)

## 웹 헤더 — Android 앱(Play 비공개 테스트) 링크

- **범위:** `site-header.tsx`, `header-android-app-link.tsx`, `android-app-icon.tsx`, `env.ts`, `.env.example`
- **할 일:** 헤더 Android 아이콘 → `https://play.google.com/apps/testing/app.bookfolio.seogadam`
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 웹 책 추가 화면 — 레이아웃 너비 확대

- **범위:** `apps/web/src/app/dashboard/books/new/page.tsx`
- **할 일:** `max-w-2xl` → 대시보드 공통 `max-w-6xl` 셸·헤더 블록 정렬
- **상태:** `done` (채팅에서 즉시 구현 지시)

## pnpm audit 보안 취약점 수정

- **범위:** `apps/web/package.json`, 루트 `package.json` (`pnpm.overrides`), `pnpm-lock.yaml`
- **할 일:** next·postcss·fast-xml-parser 업그레이드, cloudinary·supabase 전이 lodash·ws override
- **상태:** `done` (채팅에서 즉시 구현 지시)

## 모바일 — 중복 등록 시 진단 오류 대신 안내

- **범위:** `bookfolio_api.dart`, `bookfolio_client_error_reporter.dart`, `book_form_screen.dart`
- **할 일:** `BookfolioApiException`(409 등)은 전역 오류 리포트 제외·스낵바만 표시
- **상태:** `done` (채팅에서 즉시 구현 지시)
