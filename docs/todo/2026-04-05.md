# 2026-04-05

## 모바일 회원가입·로그인 화면 (HTML 목업 반영)

- **범위:** `apps/mobile` — `sign_in_screen.dart`, `auth_controller.dart`, `pubspec.yaml`
- **할 일:** Newsreader/Inter 타이포, 에디토리얼 색·레이아웃, 가입 폼(닉네임·선택 성별·생일·아바타), Google/Kakao(비활성 안내), 가입 후 자동 로그인·프로필·아바타 연동
- **상태:** `done`

## 모바일 로그인 화면 (별도 HTML 목업)

- **범위:** `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** 로그인 전용 카드·헤더·배경 블러·구글 컬러 로고·카카오 세로 스택·비밀번호 찾기 링크
- **상태:** `done`

## 모바일 프로필·하단 내비 (Bibliotheca Archive HTML 목업)

- **범위:** `apps/mobile` — `profile_screen.dart`, `main_shell_screen.dart`, `library_analysis_screen.dart`, `mobile_scroll_padding.dart`(선택)
- **할 일:** 프로필 에디토리얼 레이아웃·앱바(메뉴·타이틀·아바타)·하단 5탭(홈·서가·탐색·통계·프로필)·FAB 제거
- **상태:** `done`

## 모바일 DESIGN.md 정렬

- **범위:** `bookfolio_design_tokens.dart`, `bookfolio_themes.dart`, `sign_in_screen.dart`, `book_grid_card.dart`
- **할 일:** 노 라인·디바이더, 고스트 보더, muted gold 포커스, 타이포·반경·Ink 그라데이션, 카드 톤 레이어링
- **상태:** `done` — 사용자 규칙 적용 요청

## 모바일 홈 화면(에디토리얼 벤토) · 집계는 통계 탭

- **범위:** `apps/mobile` — `main_shell_screen.dart`, 신규 `bookfolio_home_screen.dart`, `library_analysis_screen.dart`, `bookfolio_aggregate_screen.dart`·집계 위젯 분리
- **할 일:** 홈 탭 = HTML 참고 벤토 레이아웃(환영·도서추가+공동서가·읽는중·서가현황·읽기전 추천 그리드·이달 요약), 기존 북폴리오 집계는 통계 탭 하단에 포함, 빠른 이동(내 서가·공동서가)
- **상태:** `done`

---

## 서가담·Stitch1 통합 계획 (모바일·웹·DB)

**근거:** Cursor 계획 `stitch1_화면_매핑·db_a41e115a.plan.md`. 워크플로에 따라 **채팅에서 본 섹션 `approved` 후** 구현 착수.

### 공통 전제

- 프로젝트명 **Seogadam**, 사용자 노출 **서가담**. 디자인 기준: `#Reference/Mobile_Design/stitch1` + `editorial_reserve/DESIGN.md`.
- 기능 개발과 별도 축으로 **모바일 기존 전 화면** Stitch1 목업에 맞춰 **전면 UI 패스**.

---

### Stitch1 전면 UI 패스

- **범위:** `apps/mobile/lib/src/ui/**` (쉘·홈·서가·상세·프로필·로그인·공동서가·통계·드로어·베스트/신간 등)
- **할 일:** 목업 갭 분석 → 레이아웃·타이포·토큰·컴포넌트 순차 정렬; 권장: 쉘 → 홈 → 서가 → 상세 → 프로필 → 기타
- **상태:** `done`(1차 패스) — 2026-04-07: 솔리드 `Divider` 제거·토큰 `sectionGap*`·홈 환영 Newsreader·내비 아이콘 22px·집계 카드 톤 레이어·통계 화면 타이포; 베스트/신간·상세 등 미세 조정은 이후 스프린트 가능

### 브랜딩·DESIGN.md 감사

- **범위:** `apps/mobile` 카피·앱바·스플래시·스토어; `bookfolio_design_tokens.dart` 등
- **할 일:** 표면 문구 서가담화; 패키지 `bookfolio_*` 유지; DESIGN.md 토큰 감사
- **상태:** `done`(모바일 1차) — 2026-04-07: Android `app_name`·iOS `CFBundleDisplayName`·`pubspec` 설명·집계/허브/스낵바/가입 힌트 이메일 등 서가담화; `bookfolio_design_tokens`에 Stitch1·`.cursor/rules/DESIGN.md` 상호 참조·간격 토큰

### 발견 탭·내 서가 검색 분리

- **범위:** `main_shell_screen.dart`, `discover_screen.dart`, `library_screen.dart`, `library_browse_tab.dart`, `apps/web` API·`0031` RPC
- **할 일:** 탐색 = 발견 퍼스트; 베스트/신간 더보기 TOP 30; 「다른 회원이 추가한 도서」(내 미소장 ∧ 타인 `user_books` 존재); 내 서가 검색은 **서가 탭**
- **상태:** `done` — 2026-04-05: `DiscoverScreen`·서가 「요약|내 책」·`GET /api/me/discover/community-books`·RPC `list_discover_community_books` (`0031`)

### 온보딩 (회원가입 후 1회)

- **범위:** `apps/mobile` 인증·온보딩 화면, `app_profiles`, `profile_screen.dart`
- **할 일:** `onboarding_completed_at` 권장; 카메라/바코드 안내; 프로필에 재보기
- **상태:** `done` — 2026-04-06: `0032`, `OnboardingScreen`, `POST profile` `onboardingCompleted`, 프로필 「온보딩 가이드」

### 리포트·SNS 해금

- **범위:** `policies_json`/구독 caps/정책 CMS, 웹 집계 API, 모바일 통계·리포트 UI
- **할 일:** 월간·연간·성장·SNS capability·문구; 잠금 UI; `reading_report_snapshots` 검토·도입
- **상태:** `draft` — **부분(2026-04-05):** 기존 정책 CMS = 관리자 **포인트 · 정책**의 구독 플랜 `caps_json.features` 편집기 + `getActiveSubscriptionFeatures` + 마이그레이션 `0035`(`reading_reports_unlocked`, `sns_share_unlocked`를 `vip_standard`에 시드). 앱 잠금 UI·집계 API는 미착수.

### 비소장 상세·구매 링크

- **범위:** `apps/web`, `apps/mobile` 비소장 상세; `book_purchase_offer_cache` 마이그레이션
- **할 일:** 모바일·웹 **회원만**; 알라딘+네이버 가격 행, 탭 시 알라딘/교보 웹; 캐시 테이블 즉시; 한줄평 회원 API
- **상태:** `done`(MVP) — 2026-04-08: `0033`·`GET /api/me/canon-books/:bookId/purchase-offers|community-one-liners`·모바일 `CanonBookDetailScreen`·발견 탭 탭/길게누름·웹 비소장 상세 `CanonPurchaseLinksCard`; 선택 env `ALADIN_TTB_KEY`(ItemLookUp)

### 오프라인 동기 (SQLite·아웃박스·배치 POST)

- **범위:** 모바일 SQLite/Drift, 웹 배치·멱등·이벤트·부분 갱신 API
- **할 일:** 소장 전체 미러 + `books` 메타만 + 이벤트·메모·별점; 표지 오프라인 문구; 공동서가 제외; 배치 `POST`; 동기 진행 UI·포그라운드 완료 대기; API 타임아웃·실패 기반 오프라인; 토큰 만료 → 로그아웃(아웃박스 보존); 포인트는 서버 확정 전 증감 없음
- **상태:** `approved` — 2026-04-08: 채팅에서 진행 지시. **진행:** Drift v1 스키마(`cached_user_books`·`library_cache_meta`·`sync_outbox_entries`)·기본 종이책 목록(필터 없음) 수신 시 SQLite 미러·로그아웃 시 미러만 삭제. **남음:** 아웃박스 적재·웹 배치 POST·오프라인 UI·전체 동기·JWT `sub`≠프로필 `id` 시 파티션 정합

### 정책 CMS·포인트 안내

- **범위:** `app_content_pages` 또는 `app_policy_documents`, 웹 GET, 모바일
- **할 일:** 서가 확장·해금·포인트·오프라인 포인트 배제 문구 관리자 편집
- **상태:** `draft`

### 네트워크 필수 진입점 표·가드

- **범위:** `apps/mobile` 화면·라우트
- **할 일:** 표 작성 + `OfflineMessage` 가드 전수 리뷰
- **상태:** `done`(루트 가드) — 2026-04-06: `ConnectivityController`, `NetworkGate`, `OfflineMessageScreen`; 하위 푸시 라우트는 미래 보강

### 클라이언트 오류 수집 API

- **범위:** 웹 `POST`, `client_error_reports`, `bookfolio_api` 훅
- **할 일:** API 실패 시 오류+앱·기기 메타 수집; PII 마스킹·레이트 리밋; Bearer 우선
- **상태:** `done`(MVP) — 2026-04-10: `0036`, `POST /api/client-errors`(익명·세션·Bearer `user_id` 연결, IP 해시 기준 시간당 60건, context 마스킹), 관리자 `/dashboard/admin/client-errors`

### (선택) 네이티브 크래시 SDK

- **범위:** 모바일
- **할 일:** Sentry / Crashlytics 등 — API 오류 수집과 별도
- **상태:** `draft`

### 로그인 화면 카카오 공식 버튼 이미지

- **범위:** `apps/mobile/pubspec.yaml`, `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`, 에셋 `apps/mobile/web/icons/kakao_login_medium_narrow.png`
- **할 일:** `pubspec.yaml`의 `flutter.assets`에 `web/icons/kakao_login_medium_narrow.png` 등록; 로그인 카드·회원가입 카드의 카카오 영역을 공식 PNG(`Image.asset` + `InkWell` 등)로 교체. OAuth 연동 전까지 탭 시 **「카카오 로그인은 준비 중입니다.」** 스낵바 유지.
- **상태:** `done` — 2026-04-05

### 웹·모바일 비밀번호 로그인 — 이메일 또는 @ 앞 아이디

- **범위:** `apps/web` `login-form.tsx`, `auth.config.ts`, `verify-app-user-credentials.ts`, `apps/mobile` `sign_in_screen.dart`, `supabase/migrations/0033_*`, `docs/bookfolio-db-schema.md`
- **할 일:** 로그인 UI 라벨·힌트, 서버에서 로컬 부분 → 이메일 해석(RPC)
- **상태:** `done` — 2026-04-05: 채팅 직접 구현 지시

---

## 웹 랜딩·전역 크롬 — `#Reference/Web_Design` + 서가담 브랜딩

- **범위:** `apps/web` — `globals.css`, `layout.tsx`, `page.tsx`, `site-header.tsx`, `site-footer.tsx`, `site-header-mobile-nav.client.tsx`, `login-form.tsx`, 공개 메타·파비콘, 집계·대시보드 등 사용자 노출 문구
- **할 일:** 에디토리얼 색·타이포(Manrope+Newsreader, Pretendard 폴백), 노 라인 구역 분리, 랜딩 히어로·벤토형 요약; 표기 **서가담**(코드·경로명 Seogadam 유지 가능)
- **상태:** `done` — 2026-04-05: 랜딩·`globals`·헤더·푸터·로그인·집계·법적 페이지 메타 등 서가담·에디토리얼 반영
