# 2026-04-29

- 제목: Android 15 edge-to-edge 경고 대응
  - 범위(경로/모듈): `apps/mobile/android/app/src/main/kotlin/app/seogadam/mobile/MainActivity.kt`, `apps/mobile/android/app/src/main/AndroidManifest.xml`
  - 할 일 요약: Android 15(targetSdk 35)에서 edge-to-edge 기본 동작에 맞춰 액티비티의 시스템 바 인셋 처리를 활성화하고 Flutter 임베딩 메타데이터를 명시한다.
  - 상태: `done` (사용자 지시 승인 간주 후 구현 완료)

- 제목: 모바일 카카오 로그인 콜백 복귀 안정화
  - 범위(경로/모듈): `apps/mobile/android/app/build.gradle.kts`, `apps/mobile/android/app/src/main/AndroidManifest.xml`
  - 할 일 요약: 카카오 SDK 로그인 완료 후 앱으로 복귀할 수 있도록 Android URL 스킴 콜백 설정을 추가한다.
  - 상태: `done` (채팅에서 사용자 승인: "좋아")

- 제목: iOS 카카오 로그인 콜백 설정 반영
  - 범위(경로/모듈): `apps/mobile/ios/Runner/Info.plist`, `docs/auth/kakao-mobile-login.md`
  - 할 일 요약: 카카오 SDK 로그인 후 iOS 앱으로 복귀하도록 URL scheme/queries 설정과 운영 문서를 보강한다.
  - 상태: `done` (채팅에서 사용자 승인: "IOS도 동일하게 처리해줘")
