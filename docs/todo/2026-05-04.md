# 2026-05-04

## 모임서가 상세 UI — 대시보드 정렬

- **범위:** `apps/web/src/app/libraries/[libraryId]/`, 관련 컴포넌트
- **할 일:** 좌측 컬렉션 제거, 정보 패널·회원별 읽는 책·합산 독서통계·이벤트 캘린더 2열·소장 표지 그리드+선반, 편집 페이지, 멤버 탈퇴 워딩
- **상태:** `approved` (사용자 본 턴 구체 구현 지시로 승인 간주)

## 모임서가 수정 — 대표 이미지 Cloudinary 업로드

- **범위:** `library-edit-form.client.tsx`, 신규 업로드 필드 컴포넌트
- **할 일:** 프로필 아바타와 같이 붙여넣기·파일·원격 URL → `/api/upload`(kind `cover`)
- **상태:** `approved` (사용자 지시)

## 모임서가 — 직접 책 추가 제거

- **범위:** `libraries/[libraryId]/page.tsx`, `library-detail-info-panel`, `books/new` 라우트
- **할 일:** UI·카피 정리, 내 서가 연결 안내, 구 `/books/new` 리다이렉트
- **상태:** `approved` (사용자 지시)

## 모임서가 수정 — 팝업

- **범위:** `library-edit-dialog.client.tsx`, `library-edit-form`, `library-detail-info-panel`, `edit/page.tsx`
- **할 일:** 상세에서 다이얼로그로 수정, `/libraries/:id/edit`는 상세 리다이렉트
- **상태:** `approved` (사용자 지시)

## 모임서가 도서 표시 — 내 서가 `EditorialGrid` 단일

- **범위:** `[libraryId]/page.tsx`, `dashboard-editorial-book-grid`, `library-aggregated-book-display`, 구 `library-book-cover-grid`·`library-bookshelf` 제거
- **할 일:** 표지/선반 이중 UI 제거, 책등(세로) 없이 내 서가와 동일 벽걸이 표지+목선반만
- **상태:** `approved` (사용자 지시)

## 모임서가 — 일정·참석 집계

- **범위:** `library-events.ts`, `LibraryEventsCalendar`, `LibraryEventSummary`(`@bookfolio/shared`)
- **할 일:** 멤버 일정 추가, 일정별 참석·미정·불참·응답 전 집계 표시
- **상태:** `approved` (사용자 본 턴 구체 구현 지시)

## 모임서가 도서 상세 — 대시보드 형태 / 비소유는 도서정보만

- **범위:** `libraries/[libraryId]/books/[bookId]/page.tsx`, `LibraryAggregatedBookRow`, `BookCanonInfoPanel`, `BookOneLinersInCanonPanel`
- **할 일:** 내 `user_books`가 있으면 내 서가 상세와 유사 UI; 없으면 캐논(도서 정보)만·한줄평 읽기 전용
- **상태:** `approved` (사용자 본 턴 구체 구현 지시)

## 발견 허브·알라딘 목록 — 클릭 시 캐논 상세(도서정보·한줄평)

- **범위:** `discovery/books/`, `discovery-hub-tiles.tsx`, `aladin-ttb-item-list-view.tsx`, `discovery/personalized/page.tsx`, `lib/books/repository.ts`(ISBN 변형 조회·`resolveCanonicalBook` 출처 확장), `book-canon-info-panel.tsx`(빈 값 표기)
- **할 일:** ISBN·추천 `bookId`로 상세; `books` 없으면 ISBN Open API 조회 후 upsert; 누락 필드는 `준비중`
- **상태:** `approved` (본 턴 사용자 구체 구현 지시로 승인 간주)

## 서가담 집계 — 소장 책 TOP 카드 → 상세 페이지 이동

- **범위:** `bookfolio-aggregate/page.tsx`, `findMyUserBookIdsByCanonicalBookIds`, 비소장은 `/discovery/books/[bookId]`
- **할 일:** 카드 클릭 시 내 서가면 `/dashboard/books/:userBookId`, 아니면 발견 캐논 상세
- **상태:** `done` (2026-05-04 사용자 요청으로 인라인 상세 대신 링크)

## 공지사항·개인 알림 테이블 / 헤더 / 관리 API

- **범위:** `supabase/migrations/0044_*`, `docs/bookfolio-db-schema.md`, `lib/site/*`, `api/announcements`, `api/me/notifications`, `api/admin/announcements`, `SiteHeader`, `/announcements`, `/admin/announcements`
- **할 일:** `site_announcements`·`user_notifications` 테이블, 헤더 공지·알림 UI, 공지 목록 페이지, 관리자 공지 CRUD API·화면
- **상태:** `done` (2026-05-04 구현 반영)

## 관리 공지 화면 — 개인 알림 발송

- **범위:** `admin/announcements/*`, `lib/site/resolve-admin-notification-recipient.ts`, `lib/auth/app-users.ts`(이메일·ID 조회), `api/admin/user-notifications`, `docs/bookfolio-db-schema.md`(`user_notifications` 설명)
- **할 일:** 공지 관리 페이지에서 `user_notifications`만 생성·전역 공지와 분리·ADMIN API
- **상태:** `done` (2026-05-04 사용자 지시 반영)

## 사용자 관리 — 프로필 일괄 저장·작업 열(알림·VIP)

- **범위:** `admin/users/page.tsx`, `user-row-profile-form.client.tsx`, `user-row-actions.client.tsx`, `admin/actions.ts`, 공지 페이지에서 개인 알림 제거
- **할 일:** 이름·권한 저장 단일화, 알림 다이얼로그, VIP 부여/해제 버튼, 상단 VIP 폼 제거
- **상태:** `done` (2026-05-04)

## 비로그인 헤더 — 브랜드 PNG 로고 확대

- **범위:** `apps/web/src/components/layout/site-header.tsx`
- **할 일:** `h-8`·`md:max-w-[8.5rem]` 제약 완화로 시각 크기 확대(`h-14` 행 내 여유 활용)
- **상태:** `done` (2026-05-04 본 턴 반영)
