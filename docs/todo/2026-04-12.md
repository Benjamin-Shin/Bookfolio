# 2026-04-12

## 웹 개인정보처리방침·모바일 — 카메라 안내

- **범위:** `apps/web/src/app/privacy/page.tsx`, `apps/mobile/lib/src/ui/screens/camera_permission_rationale_screen.dart`(신규), `book_form_screen.dart`, `book_detail_screen.dart`
- **할 일:** 방침에 모바일 카메라(바코드·OCR) 처리 설명 추가; 카메라 권한 직전 안내 전용 화면·진입 연동
- **상태:** `done` (채팅 직접 지시)

## 웹 랜딩 — 비회원 전용·심플 UI·루트·로고는 로그인 시 내 서재

- **범위:** `apps/web/src/app/page.tsx`, `apps/web/src/components/layout/site-header.tsx`
- **할 일:** 랜딩 카피·섹션 대폭 축소(세련된 미니멀); 로그인 세션 시 `/` → `/dashboard` 리다이렉트; 헤더 로고 링크도 세션 시 `/dashboard`
- **상태:** `done` (채팅 직접 지시)

## 모바일 로그인 — 구글 버튼 톤 다운

- **범위:** `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** 구글 로그인 `OutlinedButton` 배경을 투명보다 한 단계 어두운 서피스로 조정
- **상태:** `done`

## 모바일 로그인 — 구글 버튼 #F1F3F5·카카오와 동일 너비

- **범위:** `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** 구글 버튼 배경 `#F1F3F5`, 높이·너비 카카오 슬롯과 동일; 가입 화면 가로 2열 해제
- **상태:** `done`

## 모바일 회원가입 — 소셜 버튼·구분 문구 로그인과 동일

- **범위:** `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** 가입 화면 카카오·구글 블록을 로그인과 같은 문구·간격·위젯으로 통일
- **상태:** `done`

## 모바일 전역 — `#Reference/DESIGN.md`(서가담 에디토리얼) 정렬

- **범위:** `apps/mobile/lib/src/theme/bookfolio_design_tokens.dart`, `bookfolio_themes.dart`, 하단 네비·입력·카드 등 공통 UI, 화면별 `Divider`/`Border`·하드코딩 색
- **할 일 요약:** 문서의 서피스(`#fcf9f3` / `#f6f3ed` / `#fff`), 프라이머리 `#163826`, 메타데이터용 `secondary` `#675d53`, 세컨더리 컨테이너 `#efe0d4`, 본문·라벨을 **Manrope**(DESIGN §3), CTA 그라데이션·고스트 보더·앰비언트 섀도 규칙, **노 라인/글래스 바**(§2·§5)에 맞춰 토큰·테마 갱신 후 위젯 일괄 점검
- **상태:** `done`

## 모바일 — Dart 패키지 `seogadam_mobile`·표시명 서가담 정렬

- **범위:** `apps/mobile/lib/**`, `test/`, Android `namespace`/MainActivity, iOS `CFBundleName`, `docs/auth/google-mobile-login.md`
- **할 일:** `package:bookfolio_mobile` → `package:seogadam_mobile`; Android `app.seogadam.mobile`; iOS `CFBundleName` `seogadam_mobile`(표시는 `CFBundleDisplayName` 서가담 유지)
- **상태:** `done` (채팅 직접 지시)

## 모바일 로그인 — 주 버튼·소셜 버튼 너비를 카카오 PNG 비율에 맞춤

- **범위:** `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** `kakao_login_medium_narrow.png`(183×45) 왜곡 없이 두기 위해, 로그인·구글 버튼 너비를 동일 비율로 줄여 가운데 정렬(가입 주 버튼 높이 52로 통일)
- **상태:** `done` (채팅 직접 지시)

## 웹 로그인 — 카카오 PNG·모바일과 동일 너비·순서

- **범위:** `apps/web/src/components/auth/login-form.tsx`
- **할 일:** `/assets/kakao_login_medium_narrow.png` 추가, 주 버튼·카카오·구글을 183×45 비율 너비 컬럼에 가운데 정렬; 이메일 폼 아래 배치(OAuth 미연동 시 안내)
- **상태:** `done` (채팅 직접 지시)

## 모바일 내 서재 — 도서 추가 버튼

- **범위:** `apps/mobile/lib/src/ui/screens/library_screen.dart`
- **할 일:** 요약/내 책 세그먼트 옆(상단)에 `BookFormScreen`으로 이동하는 도서 추가 버튼 배치, 반환 후 목록·허브 새로고침
- **상태:** `done` (채팅 직접 지시)

## 모바일 — 내 서재 요약 탭 제거·이벤트 캘린더 홈 이전

- **범위:** `apps/mobile/lib/src/ui/screens/library_screen.dart`, `bookfolio_home_screen.dart`, `apps/mobile/lib/src/ui/widgets/reading_events_calendar_card.dart`(신규)
- **할 일:** 「요약」세그먼트·허브 UI 제거 후 서재는 목록만; 독서 이벤트 캘린더는 재사용 위젯으로 홈(빠른 이동 아래)에 배치·당김 새로고침 연동
- **상태:** `done`

## 모바일 홈 — `GET /api/me/mobile-home` 묶음 응답

- **범위:** `apps/web/src/app/api/me/mobile-home/route.ts`, `apps/web/src/lib/mobile/mobile-home-bundle.ts`(신규), `apps/mobile/lib/src/services/bookfolio_api.dart`, `apps/mobile/lib/src/ui/screens/bookfolio_home_screen.dart`
- **할 일:** 프로필·서재 요약·포인트·읽는 중 1권·읽기 전 8권을 한 요청으로 조회; 홈 `_load`는 해당 API만 호출
- **상태:** `done`

## 웹 대시보드 — 내 서재 스티치 HTML 목업 UI + 소장 제목순 정렬

- **범위:** `apps/web/src/app/dashboard/page.tsx`, `apps/web/src/app/globals.css`, `apps/web/src/components/dashboard/*`, `apps/web/src/lib/books/repository.ts`, `apps/web/src/lib/dashboard/dashboard-href.ts`, `supabase/migrations/0037_list_user_books_paged_title_sort.sql`, `docs/bookfolio-db-schema.md`
- **할 일:** 에디토리얼 레이아웃(사이드·Hall of Fame·목판 선반 그리드·FAB), URL `sort=title`·RPC `p_sort`
- **상태:** `done` (채팅 직접 지시)

## 웹·모바일 로그인/가입 — 구글 공식 버튼 에셋 (`#Resources/google-signin-assets`)

- **범위:** `apps/web/public/assets/`, `apps/web/src/components/auth/login-form.tsx`, `apps/mobile/web/icons/`, `apps/mobile/pubspec.yaml`, `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`
- **할 일:** Web(light·SI/SU)·Android(light·rd·SI/SU) PNG를 복사해 카카오와 동일 슬롯(`object-contain`, 높이 52)으로 표시; 로그인 탭·가입 탭에 각각 Sign in / Sign up 에셋 적용
- **상태:** `done` (채팅 직접 지시) (채팅 직접 지시)

## 웹 대시보드 — 내 서재 UI·Hall of Fame 전용·선반 정렬

- **범위:** `apps/web/src/app/dashboard/page.tsx`, `apps/web/src/components/dashboard/dashboard-books-toolbar.tsx`, `apps/web/src/lib/books/repository.ts`, `supabase/migrations/0038_list_user_books_paged_hall_of_fame.sql`, `docs/bookfolio-db-schema.md`
- **할 일:** 사이드 메뉴 권수·상단 중복 탭·요약 카드·가격 합계 제거; ALL(소장) 탭에서만 검색; Hall of Fame(완독·평점 4+)만 목록; 커버 업스케일 방지·선반 위치 재배치; 내 서재 제목 크기 축소; RPC `p_hall_of_fame`
- **상태:** `done` (채팅 직접 지시)

## 웹 대시보드 — 컬렉션 권수·탭 목록 vs Hall of Fame 분리

- **범위:** `apps/web/src/app/dashboard/page.tsx`
- **할 일:** ALL·Reading·To Read·Completed는 소장/읽기상태 전체 집계·목록; Hall of Fame만 `p_hall_of_fame`; 빈 서재는 소장 0권 기준
- **상태:** `done` (채팅 직접 지시)

## 웹 대시보드 — Hall of Fame `tab=hall`·선반·페이지네이션

- **범위:** `apps/web/src/app/dashboard/page.tsx`, `apps/web/src/lib/dashboard/dashboard-href.ts`, `apps/web/src/components/dashboard/dashboard-books-toolbar.tsx`
- **할 일:** HoF는 사이드 메뉴 클릭 시에만 표시; 에디토리얼 선반 그리드+`DashboardBooksPagination`
- **상태:** `done` (채팅 직접 지시)

## 모바일 Android — `applicationId` Play Console(`app.bookfolio.seogadam`) 정렬

- **범위:** `apps/mobile/android/app/build.gradle.kts`, (참고) Google Cloud OAuth Android 클라이언트·`docs/auth/google-mobile-login.md`
- **할 일:** `applicationId`를 스토어 등록 패키지 `app.bookfolio.seogadam`과 동일하게 설정; 구글 로그인용 클라이언트 ID는 동일 패키지명·SHA-1로 재확인
- **상태:** `done` (채팅: Play 등록 패키지명 기준 직접 지시)

## 웹 헤더 — 모바일 상단 중앙 `Seogadam_Web_logo.png`

- **범위:** `apps/web/src/components/layout/site-header.tsx`
- **할 일:** `md` 미만에서 브랜드 영역을 뷰포트 가운데 정렬; 에셋 경로를 `Seogadam_Web_logo.png`로 정정; 접근성용 `alt`
- **상태:** `done` (채팅 직접 지시)

## 웹 공동서재 — 내 서재형 좌측 패널·집계 탭·멤버(아바타)·초대 팝업

- **범위:** `apps/web/src/app/dashboard/libraries/page.tsx`, `apps/web/src/app/dashboard/libraries/[libraryId]/page.tsx`, `apps/web/src/components/libraries/*`, `apps/web/src/lib/libraries/repository.ts`, `packages/shared/src/contracts.ts`
- **할 일:** 상세에 ALL·Reading·To Read·Completed·Hall of Fame(멤버 기준)·멤버 목록·초대 다이얼로그; 허브 페이지 동일 셸; `user_books.rating`으로 HoF 판별
- **상태:** `done` (채팅 직접 지시)

## 웹 대시보드 — 베스트셀러·초이스 신간 카드 클릭 시 알라딘 이동

- **범위:** `apps/web/src/components/dashboard/aladin-ttb-item-list-view.tsx` (공통), `bestsellers`·`choice-new` 페이지는 동일 뷰 사용
- **할 일:** 항목 `link`로 새 탭 열기; 링크 없을 때 ISBN 기반 알라딘 상품 URL 폴백
- **상태:** `done` (채팅 직접 지시)
