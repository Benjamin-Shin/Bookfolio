# 2026-04-08

## 오프라인 동기 1차 (로컬 미러·아웃박스 기반)

- **범위:** `apps/mobile` — Drift SQLite, `LibraryController`, `main.dart`
- **할 일:** JWT `sub` 기준 `cached_user_books`·`library_cache_meta`·`sync_outbox` 테이블; 기본(검색/상태 필터 없음) 종이책 목록 성공 시 캐시; 로그아웃 시 미러만 삭제(아웃박스 보존); 웹 배치 POST·동기 UI는 후속
- **상태:** `done`(1차 스키마+캐시) — `docs/todo/2026-04-05.md` 통합 계획 항목과 연계
- **추가(2026-04-08):** 내 책 목록 요청 `TimeoutException` 시 확인 다이얼로그(확장 설명)·SQLite 저장 목록 표시·배너
