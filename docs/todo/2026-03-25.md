# 2026-03-25

## 알라딘 베스트셀러(목록) 대시보드·모바일

- **범위**: `apps/web` (`lib/aladin`, `app/api/me/aladin-bestseller`, `app/dashboard/bestsellers`), `apps/mobile` (화면·API 클라이언트)
- **할 일**: `ALADIN_BESTSELLER_API_BASE_URL`로 알라딘 ItemList(XML·JSON) 조회, 웹 대시보드 페이지·모바일에서 인증 API로 동일 데이터 표시
- **상태**: `done`

## 대시보드 사이드 카드 프로필 아바타

- **범위**: `apps/web/src/app/dashboard/page.tsx`
- **할 일**: `session.user.image`가 있으면 이름·이메일 옆에 원형 아바타 표시
- **상태**: `done`

## 알라딘 초이스 신간(목록) 대시보드·모바일

- **범위**: `apps/web` (`ALADIN_ITEMNEW_API_BASE_URL`, `app/api/me/aladin-item-new`, `app/dashboard/choice-new`), `apps/mobile` (`fetchAladinItemNewFeed`, `ChoiceNewScreen`, 내 서가 앱바)
- **할 일**: 베스트셀러와 동일 피드 스키마로 신간 ItemList 노출
- **상태**: `done`

## 모바일 모임서가 UI·공동 도서 상세·관리자 알라딘 빠른 추가

- **범위**: `apps/mobile` (모임서가 목록·책 그리드·`SharedLibraryBookDetailScreen`, 공통 그리드 위젯), `apps/web` (`dashboard/admin/books`, `admin-prefill`, `books/new` 쿼리)
- **할 일**: 모임서가를 내 서가와 유사한 그리드/앱바로 표시; 내·공동 서가에서 탭 시 도서 상세(공동은 읽기 전용); 관리자 도서 목록에서 알라딘 목록으로 신규 등록 폼 프리필
- **상태**: `done`

## 베스트셀러 카테고리 뱃지 UI

- **범위**: `apps/web/src/app/dashboard/bestsellers/page.tsx`
- **할 일**: `categoryName`을 `>` 기준 분리해 표지·제목 영역 상단에 뱃지 표시
- **상태**: `done`

## 알라딘 제목 XML 엔티티 디코딩

- **범위**: `apps/web/src/lib/aladin/bestseller-feed.ts`
- **할 일**: `&lt;` 등 이스케이프된 제목·문자열 필드 복원
- **상태**: `done`

## app_users.policies_json · 모임서가 생성 상한

- **범위**: `supabase/migrations/0017_*`, `packages/shared` (`AppUserPolicies`), `lib/auth/app-user-policies`, `lib/libraries/repository`·`shared-library-policy`, API·모임서가 UI
- **할 일**: JSON 정책 컬럼, `sharedLibraryCreateLimit` 기본 1·생성 시 검증
- **상태**: `done`

## Cloudinary 표지 업로드 · 도서 신규/수정 연동

- **범위**: `apps/web` (`app/api/upload/route.ts`, `components/books/book-cover-upload-field.tsx`, `new-book-form.tsx`, `dashboard/books/[id]/edit/page.tsx`, `api/me/books/[id]/route.ts`), `.env.example`
- **할 일**: 업로드 API 린트·인증·URL 가져오기 지원; 신규/수정 폼에서 클립보드·파일·원격 URL로 업로드 후 `coverUrl` 반영
- **상태**: `done`

## Textarea hydration 경고(확장 속성)

- **범위**: `apps/web/src/components/ui/textarea.tsx`
- **할 일**: 확장 프로그램 주입 속성과의 SSR/CSR 불일치 완화 — `suppressHydrationWarning`
- **상태**: `done` (사용자 재현 스택 기준 직접 수정)

## 도서 수정 페이지 표지 미리보기·비-Cloudinary 이관

- **범위**: `apps/web/src/lib/books/cover-url.ts`, `components/books/book-cover-upload-field.tsx`, `dashboard/books/[id]/edit/page.tsx`
- **할 일**: 표지 URL 있으면 미리보기; Cloudinary가 아니면 현재 URL·클립보드·파일로 업로드 UX 보강
- **상태**: `done` (사용자 직접 지시)
