# TODO — 2026-03-20

## 항목

### 1. 개발 워크플로 규칙 도입

- **범위**: `.cursor/rules/workflow-todo-approval.mdc`, `docs/todo/**`
- **요약**: (1) 추가·수정 시 `docs/todo/YYYY-MM-DD.md`에 기록 (2) 사용자 검토·승인 후 코드 변경 (3) 함수·클래스 등에 JSDoc `@history` 기록
- **상태**: `done`
- **승인**: 사용자가 본 대화에서 규칙 추가를 직접 요청함 (2026-03-20)

### 2. HTTP 메서드 GET·POST만 사용 규칙

- **범위**: `.cursor/rules/web.mdc` (API Route 섹션)
- **요약**: `PUT` / `PATCH` / `DELETE` 금지, `GET`·`POST`만 허용. 수정·삭제는 `POST`로 표현.
- **상태**: `done`
- **승인**: 사용자가 본 대화에서 규칙 문서 반영을 직접 요청함 (2026-03-20)

**참고**: 현재 코드베이스 일부 API는 아직 `PATCH`/`DELETE`를 쓰고 있으므로, 이후 변경 시 본 규칙에 맞게 `POST`로 옮기면 된다.

---

### 3. 모바일(Flutter) — 웹 대시보드와 동일한 책장·ISBN·검색 UX

- **범위**: `apps/mobile/**`, (필요 시) `apps/web/src/app/api/me/books/route.ts`, `packages/shared` 타입 정렬
- **상태**: `draft`
- **승인**: (채팅에서 본 항목 승인 후 구현)

#### 배경·참고 (웹 기준)

- 웹 대시보드: `apps/web/src/app/dashboard/page.tsx` — **읽는 중**(`readingStatus=reading`, 최대 50권) / **소장**(`isOwned=true`, 페이지당 14권) 구분, `Bookshelf` 표지·선반 UI, 검색 `q`, 소장 가격 합계 카드.
- ISBN 조회 API: `POST /api/books/lookup-by-isbn` — 이미 **`books` 정본(catalog) 우선**(`findCanonicalBookByIsbn`) 후 외부 API (`lookupBookByIsbn`). 모바일은 동일 엔드포인트만 쓰면 됨.
- 모바일 목록 API: 현재 `GET /api/me/books` 전체 배열. **페이지네이션 분기**(`?page=&pageSize=&q=`)는 있으나 **`isOwned` 필터가 쿼리로 노출되지 않음** — 대시보드와 동일 데이터를 REST로 받으려면 API 확장 필요.

#### 할 일 (구현 체크리스트)

1. **REST API 확장 (웹)**  
   - `GET /api/me/books` 의 `page` 모드에서 `isOwned=true|false` (또는 명시적 문자열) 쿼리 지원 → `listUserBooksPaged` 의 `p_is_owned` 와 연결.  
   - (선택) 소장 가격 합계: `getUserOwnedBooksPriceStats` 를 `GET /api/me/books/price-summary` 등 **GET 단일**로 노출해 모바일 카드와 맞춤.

2. **모델·클라이언트 (`bookfolio_api` / `LibraryController`)**  
   - `UserBookSummary` 에 가깝게 필드 정리 (`coverUrl`, `isOwned`, `priceKrw`, `createdAt` 등 API 응답과 동기화).  
   - 읽는 중 목록: `readingStatus=reading&page=1&pageSize=50` (+ 검색 `q`).  
   - 소장 목록: `isOwned=true&page&pageSize=14` (+ `q`).  
   - 검색어 변경 시 두 목록 모두 재요청; 소장만 페이지 인덱스 유지.

3. **책장 UI (표지)**  
   - 웹 `Bookshelf` / `BookOnShelf` 와 유사: 가로 스크롤 또는 줄바꿈 그리드, **표지 이미지**(`coverUrl` — `NetworkImage` + 깨짐 시 placeholder), 얇은 기울기·그림자는 선택.  
   - **읽는 중** / **소장** 섹션 제목·설명 문구는 웹과 동일 의미로 한국어 카피 정렬.  
   - 선반 톤: 웹의 sky(읽는 중) / amber(소장) 그라데이션을 Flutter `BoxDecoration` 등으로 근사.

4. **검색 (ISBN 수동 + 바코드)**  
   - 서재 화면 상단: 웹 툴바와 같이 **제목·저자** 검색은 `q`/`search` 로 두 목록(읽는 중·소장) 필터.  
   - **ISBN**(요청 사항): 웹 대시보드 툴바는 현재 ISBN 전용 입력이 없음 → 모바일에서 **ISBN 입력 + 조회** 또는 **바코드 스캔**으로 `lookupByIsbn` 호출 후, 책 메타 확인·해당 `user_books` 상세로 이동 또는 미등록 시 등록 화면으로 유도.  
   - **카메라**: `BarcodeScanScreen` + `mobile_scanner` 재사용.

5. **책 등록 화면 (`BookFormScreen`)**  
   - **ISBN 텍스트 필드** 추가(수동 입력) → 입력 후 “ISBN으로 채우기” 버튼으로 `lookupByIsbn` 호출해 메타데이터 프리필.  
   - 앱바 **바코드** 버튼은 유지(카메라).  
   - (웹 `new-book-form` 과 동일) 가격(원) 등 누락된 필드가 있으면 API `toCreatePayload` 와 맞춰 추가.

6. **ISBN 조회 순서**  
   - 별도 모바일 로직 불필요. `POST /api/books/lookup-by-isbn` 만 사용하면 **catalog → 외부** 순서는 서버가 보장.

7. **빈 상태·검색 결과 없음**  
   - 웹 카피·구조 참고: 서재 비었을 때 / 검색 결과 없을 때 메시지·CTA(책 추가).

8. **품질**  
   - 변경/신규 공개 함수·클래스에 프로젝트 규칙대로 `@history` 또는 파일 상단 이력 주석.  
   - `dart analyze` 통과.

#### 비범위 (이번 TODO에서 제외 가능)

- iOS/Android 스토어 빌드 파이프라인.  
- 웹 `PATCH` 프로필 API 정리(별도 항목).

---

### 4. 공동서재 초대 — 기존 회원만 + MVP **앱 사용 중(포그라운드) 알림**

- **범위**: `apps/web` (필요 시 **경량 알림/이벤트 API**), `apps/mobile` (포그라운드 폴링 또는 주기적 동기화 + **SnackBar / MaterialBanner** 등), (선택) `supabase/migrations`·`packages/shared`
- **상태**: `draft` (모바일 MVP 인앱 알림은 2026-03-20 구현 완료)
- **승인**: 사용자가 구현을 직접 요청함 (2026-03-20)
- **비고**:
  - **비회원 가입 유도 메일**은 아래 「4-후속」.
  - **원격 푸시(FCM/APNs)** 는 MVP에 넣지 않음 → 「4-정식화」에서 서비스 정립 후 도입.
  - **구현 요약 (모바일)**: `SharedLibraryInviteController` — `GET /api/me/libraries` 폴링(45초, 포그라운드만) + `SharedPreferences`로 알려진 `library_id` 집합 diff, SnackBar·「보기」→ `SharedLibraryBooksScreen`. 전역 `navigatorKey` / `scaffoldMessengerKey` (`app_root_scaffold.dart`).

#### 현재 상태 (코드 기준)

- 멤버 추가: `POST /api/me/libraries/[libraryId]/members` → `addLibraryMemberByEmail` — **`app_users`에 해당 이메일이 있을 때만** 성공.
- UI: `library-members-panel.tsx` — “이미 Bookfolio에 가입한 이메일만 추가할 수 있습니다.”
- 모바일: 로그인 후 **`SharedLibraryInviteLifecycle`** 로 포그라운드 폴링·SnackBar (`lib/src/state/shared_library_invite_controller.dart` 등).

#### 목표 동작 (MVP)

| 대상 | 원하는 경험 |
|------|-------------|
| 이미 `app_users`에 있는 이메일 | 기존처럼 **즉시 `library_members`에 추가**. 피초대자는 **앱을 켜 둔 상태**에서 짧은 주기로 서버와 동기화하거나 화면 진입 시 조회해, **새 서재 멤버십을 감지하면 인앱 메시지**(토스트/배너)로 “OO 서재에 초대되었습니다” 등 표시. |
| 미가입 이메일 | **변경 없음** — 기존 검증·에러 유지. 추후 「4-후속」에서 메일 초대 검토. |

**MVP 한계 (의도적)**: 앱이 **백그라운드·종료**일 때는 알림 없음 → 정식화 시 「4-정식화」 푸시로 보완.

#### 할 일 체크리스트 — 제품·정책 (MVP)

- [x] **즉시 멤버 추가** 유지 (수락 플로우는 이번에 도입하지 않음).
- [x] 인앱 알림 **중복 표시 방지**: 마지막으로 본 `joined_at` / 이벤트 ID / 로컬 캐시 등 최소 규칙.
- [ ] (선택) **알림 센터·읽음 처리**는 MVP에서 제외 가능 — 배너만으로 시작.

#### 할 일 체크리스트 — 서버 (MVP)

- [x] **구현 방식 택1 (또는 병행)**  
  - **A안**: 기존 `GET /api/me/libraries`(또는 멤버십 목록)만으로 “이전 스냅샷 대비 신규 library_id” 감지 — **추가 API 없이** 가능한지 검토.  
  - **B안**: `GET /api/me/notifications` 등 **경량 피드** (`type: library_invite`, `libraryId`, `createdAt`) — 클라이언트가 폴링하기 쉬움.  
- [ ] (B안 시) 멤버 추가 시점에 피초대자용 **이벤트 1건 기록** (테이블 또는 기존 도메인에 부가).

#### 할 일 체크리스트 — 데이터베이스 (MVP)

- [x] A안만 쓰면 **스키마 변경 없을 수 있음**.  
- [ ] B안이면 **`user_notifications` (가칭)** 등 최소 테이블 또는 `library_members.joined_at` 기반 쿼리만으로 충분한지 결정.

#### 할 일 체크리스트 — 모바일 Flutter (MVP)

- [x] **앱 라이프사이클**: `WidgetsBindingObserver` / `RouteAware` 등으로 **포그라운드 복귀** 시 또는 **N초 간격**(앱이 앞에 있을 때만) 폴링.
- [x] 신규 초대(또는 신규 멤버십) 감지 시 **SnackBar** 또는 **MaterialBanner**, 탭 시 해당 공동서재 화면으로 이동.
- [x] **배터리·요청 빈도**: 폴링 간격 상한(예: 30~60초)·화면에 있을 때만 등 정책.

#### 할 일 체크리스트 — 웹 (MVP)

- [ ] (선택) 대시보드에서 동일 **토스트** 수준 알림 — 없어도 MVP는 모바일만으로 성립 가능.

#### 비범위 (MVP)

- **FCM / APNs / 디바이스 토큰** — 「4-정식화」.
- 웹 푸시(Web Push) — 필요 시 별도.

---

### 4-정식화. (추후) 원격 푸시 서비스 (FCM 등)

- **상태**: `planned` (서비스 정식화 단계에서 착수)
- **전제**: MVP 인앱 알림으로 검증한 뒤, 백그라운드·앱 종료 시에도 도달해야 할 때 도입.

#### 할 일 체크리스트 — 인프라·앱

- [ ] **Firebase 프로젝트** (또는 APNs 직접) 생성, iOS·Android 앱 연동.
- [ ] Flutter: `firebase_core`, `firebase_messaging`, 백그라운드 핸들러, Android 알림 채널.
- [ ] **`user_push_devices`** (또는 동등) 마이그레이션 + **토큰 등록 API** (`POST`).
- [ ] **서버 → FCM**: 멤버 추가(또는 수락) 시 해당 `user_id`로 전송; 페이로드에 `libraryId`, `type` 등.
- [ ] 푸시 실패 시에도 **멤버 추가 트랜잭션은 성공**할지 정책.
- [ ] 운영: 타임아웃·재시도, 로그에 토큰 원문 금지.

---

### 4-후속. (추후) 비회원 가입 유도 — 초대 메일·대기 초대

- **상태**: `planned` (구현 시기 미정)
- **전제**: 항목 4(MVP 인앱 알림)와 별도로 착수.

#### 목표 동작 (추후)

| 대상 | 원하는 경험 |
|------|-------------|
| 미가입 이메일 | **가입/초대 메일** (딥링크 또는 토큰) → 가입 완료 시 서재 멤버로 연결 |

#### 할 일 체크리스트 — 제품·정책 (추후)

- [ ] **비회원 초대 시** 멤버 반영 규칙: (A) 가입 전 `pending_invitations`만, 가입 시 승격 (B) 이메일 일치 자동 합류 등.
- [ ] **만료·재전송**: 초대 토큰 TTL, 같은 이메일·같은 서재 중복 초대, 레이트 리밋.
- [ ] **웹만 회원** 알림: 푸시 대신 **이메일 알림** 여부.

#### 할 일 체크리스트 — 데이터베이스 (추후)

- [ ] **`library_invitations` (가칭)**: `library_id`, `email`, `invited_by`, `token_hash`/서명 토큰, `expires_at`, `accepted_at`, 유니크 제약 등.

#### 할 일 체크리스트 — 이메일 (추후)

- [ ] 발송 제공자(Resend / Postmark / SES 등)·도메인 인증(SPF/DKIM).
- [ ] 템플릿·발송·실패 시 롤백 또는 재시도.
- [ ] 가입·로그인 후 토큰 검증 API → `library_members` + `accepted_at` (세션 이메일 일치 검증 필수).

#### 할 일 체크리스트 — API·웹 UI (추후)

- [ ] `addLibraryMemberByEmail` 분기: 미가입 → invitation + 메일; UI 카피·에러 메시지 정리.
- [ ] (선택) 대기 중 초대 목록(owner).
- [ ] 응답 타입 정리 (`invited` vs `added`).

#### 보안 (추후)

- [ ] 초대 토큰 **추측 불가**; 로그에 토큰 원문 금지.
