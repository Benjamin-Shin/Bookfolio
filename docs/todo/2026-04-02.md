# 2026-04-02

## 모바일 UI 개편 — 공통 원칙

- **범위:** `apps/mobile/` UI 전반(테마·레이아웃·문구 일관화)
- **할 일:** 모든 화면을 세련되고 일관된 패턴으로 정리. **eBook은 취급하지 않으며 종이책만** 전제로 UI·카피·분기 정리(전자책 관련 노출·설정 제거 또는 비활성화 포함 검토).
- **상태:** done (2026-04-02 승인·구현: 종이책 필터·폼 고정)

## 모바일 — 하단 아이콘 메뉴 + 내서재/공동 서재 선택

- **범위:** 앱 내비게이션·쉘(예: `apps/mobile/lib/src/ui/` 내 메인 스캐폴드, 하단 내비 위젯)
- **할 일:** 메뉴를 **하단 아이콘**으로 표시. 항목: **내 서재 / 공동 서재**는 **드롭다운(또는 동급 선택 UI)**으로 전환, **검색**, **집계**. **햄버거 메뉴는 유지**.
- **상태:** done (`MainShellScreen`, 드로어·앱바 드롭다운)

## 모바일 — 내 서재 화면 (허브)

- **범위:** `apps/mobile/lib/src/ui/screens/` 내 서재 관련 화면(기존 `library_screen` 등)
- **할 일:**
  - **읽는 중** 도서를 **카드 형태로 복수** 표시. 카드 레이아웃: 상단 행 **커버 이미지 | 평점**, 그 아래 **책 제목**.
  - **이벤트 캘린더** 표시: 이벤트가 있는 날짜에 해당 **도서 커버 이미지**를 캘린더 셀에 표시(다건 시 표시 규칙 정의 필요).
  - 구분선 아래 지표 블록: **올해 읽은 책 수** `xx`권, **이번 달 읽은 책 수** `xx`권, **총 읽은 페이지 수** `xxxx`페이지.
  - **「내 서재 분석」** 진입(탭 시 해당 페이지로 이동).
- **상태:** done (`library_screen` 허브·API `personal-library-summary`)

## 모바일 — 내 서재 분석 화면 (신규 또는 확장)

- **범위:** `apps/mobile/lib/src/ui/screens/` (신규 `library_analysis_screen` 등), 필요 시 데이터 집계 레이어
- **할 일:** 다음 블록을 순서대로 표시.
  - **내 서재의 책 수** `xx`권 — 물리적 권수(실제 등록 권).
  - **내가 소장 중인 책 수** `xx`권 — 시리즈 등은 **그룹 1개 = 1권**으로 집계(위 지표와 구분).
  - **내 서재 현황** — **완독** `xx`권 / **읽기 전** `xx`권 / **인생책** `xx`권, **아이콘 + 숫자** 위주 UI.
  - **내 서재의 총 가격** — **정가** 합계 `xxx,xxx,xxx`원.
  - **내가 기록한 메모** `xxx`개.
  - **내가 기록한 한줄평** `xxx`개.
- **비고:** 시리즈 그룹핑 규칙·이벤트·정가·페이지 합계 등은 서버/DB·도메인 모델과 정합성 확인 후 구현.
- **상태:** done (`library_analysis_screen`, 웹 `GET /api/me/stats/personal-library-summary`)

---

## 신규·후속 작업 (2026-04-02 구현 완료)

### 1. 프로필 — 성별·생년월일(선택·공개) + 통계 연동 — **done** (2026-04-02 구현: `0029`, `app_profiles` API·웹·모바일, `profileForStats` 요약)

- **범위:** `docs/bookfolio-db-schema.md` 갱신, DB 마이그레이션, 웹·모바일 프로필 API/UI, 향후 통계 집계(`personal-library-summary` 등)
- **비고:** 스키마상 웹 사용자 프로필은 `public.app_profiles`, Supabase Auth 1:1는 `public.profiles`. **어느 테이블(또는 둘 다)에 컬럼을 둘지** 클라이언트·인증 경로와 맞춰 결정.
- **할 일:**
  - DB: **성별**, **생년월일** 컬럼 추가, **NULL 허용**. 값이 있어도 **공개로 설정한 경우에만** 통계·집계에 쓰도록 `*_public` 플래그 등 설계.
  - 앱/웹: 프로필 수정에서 입력·공개 여부 설정.
  - 통계: 공개 동의 항목만 활용(구체 지표는 후속).
- **상태:** done

### 2. 평점 — 소유(`user_books`) vs 도서 **회원 평균** 표시 — **done**

- **범위:** `docs/bookfolio-db-schema.md`, `user_books`·`books`, 모바일/웹 UI·API
- **스키마 확인(문서 기준):** `user_books.rating` (1–5, NULL 가능) — **개인 소장 행 단위 평점**. `books`에는 평점 컬럼 없음.
- **할 일:**
  - 코드에서 표시·저장이 위 스키마와 일치하는지 재확인.
  - 도서 카드/상세에 **모든 회원 평균**을 내려주려면: `book_id` 기준 `user_books` 집계(평균·건수), 최소 표본 수·비공개 처리, 필요 시 **집계 캐시/컬럼** 검토.
  - UI: **내 평점**과 **커뮤니티 평균** 레이블·출처 구분.
- **상태:** done (`book-community-ratings`, 모바일 상세·허브 카드)

### 3. 메모 — 음성 입력(~30초) → 텍스트 저장 — **done** (`speech_to_text`, 최대 30초)

### 3b. (구) 검토 항목 병합

- **범위:** (참고) 클라우드 STT·비용 — 현재는 OS 내장 인식.

### 4. 메모 — 작성 시각 표시 — **done**

- **범위:** 모바일 메모 UI, API에 `created_at` 등 시각 필드 포함 여부
- **할 일:** 목록/상세에 **남긴 시각** 표시(포맷은 시스템 로케일·이벤트 항목 5와 통일 권장).
- **상태:** done

### 5. 이벤트 — 시간 포맷(시스템 로케일) + 문구(`Current Page`) — **done**

- **범위:** 모바일 이벤트·캘린더·리스트의 시간 문자열, 하드코딩된 `Current Page` 등
- **할 일:** `DateFormat`/로케일 기반 포맷으로 통일. **Current Page** → **현재 페이지**(또는 앱 로케일에 맞는 동적 문자열)로 의미 명확화.
- **상태:** done (`intl` 로케일, 진도 페이로드 한글)

### 6. 모바일 — 도서 상세 UI 개편 — **done**

- **범위:** 도서 상세 화면, 관련 폼·라우팅
- **할 일:**
  - 커버 크기 축소, **제목·저자는 커버 오른쪽** 정도 레이아웃.
  - **평점**, **위치**(`user_books.location`) 상세에서 바로 편집·저장.
  - **도서 메타데이터(서지) 편집** 진입·화면 제거 — 상세에서는 개인 필드만.
- **상태:** done

### 7. 버그 — 도서 상세에서 읽기 상태 변경 후 즉시 갱신 안 됨 — **done**

- **범위:** 모바일 도서 상세, `LibraryController`(또는 동등) 상태·리스트 동기화
- **할 일:** `reading_status` 변경 후 상세·목록·캐시가 바로 반영되도록 수정.
- **상태:** done (`LibraryController` 오버라이드)

### 8. 내 서재 분석 — 같은 저자 소장 권수 TOP 3 — **done**

- **범위:** `library_analysis_screen`, `GET /api/me/stats/personal-library-summary`, 백엔드 집계(`books.authors` / `book_authors`·`authors`와 정합)
- **할 일:** 소장 `user_books` 기준 저자별 권수 상위 3명(동순위 처리·복수 저자는 `book_authors` 우선 등 규칙 확정).
- **상태:** done (`books.authors` 첫 요소 기준)
