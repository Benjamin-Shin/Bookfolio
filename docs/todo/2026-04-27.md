## 프로필 편집: 테마 제거 + 카테고리 Depth 드롭다운

- 제목: 프로필 편집 화면 테마 설정 제거 및 관심 카테고리 3단계 선택
- 범위: `apps/mobile/lib/src/ui/screens/etc/profile_edit_screen.dart`
- 할 일 요약: 사용자 직접 구현 지시에 따라 화면 설정(ThemeMode) 기능을 제거하고, 관심 카테고리 추가를 `depth1 > depth2 > depth3` 드롭다운으로 선택 가능하도록 변경
- 상태: approved

## 웹 로그인 페이지 시안 반영

- 제목: 웹 로그인 페이지 UI를 시안과 동일한 구조로 개편
- 범위: `apps/web/src/app/login/page.tsx`, `apps/web/src/components/auth/login-form.tsx`
- 할 일 요약: 사용자 요청(시안 기반 직접 구현 지시)을 승인으로 간주하고, 좌측 비주얼/우측 로그인 카드/하단 안내 영역을 포함한 로그인 페이지 스타일을 시안에 맞게 수정
- 상태: approved

## 비로그인 헤더 내 서가 버튼 숨김

- 제목: 비로그인 상태에서 헤더 내 `내 서가` 버튼 비노출
- 범위: `apps/web/src/components/layout/site-header.tsx`, `apps/web/src/components/layout/site-header-mobile-nav.client.tsx`
- 할 일 요약: 사용자 직접 구현 지시에 따라 인증되지 않은 상태에서는 데스크톱/모바일 헤더의 `내 서가` 진입 버튼을 렌더링하지 않도록 조건 분기 수정
- 상태: approved

## 로그인 페이지 헤더 로그인 CTA 숨김

- 제목: `/login` 경로에서 상단 헤더 로그인 버튼 비노출
- 범위: `apps/web/src/components/layout/site-header.tsx`, `apps/web/src/components/layout/site-header-mobile-nav.client.tsx`
- 할 일 요약: 사용자 직접 구현 지시에 따라 로그인 페이지에서는 중복 노출되는 로그인 CTA를 숨기고, 다른 비로그인 페이지에서만 로그인 CTA 노출
- 상태: approved

## 로그인 소셜 버튼 스타일 및 노출 보정

- 제목: 구글 커스텀 버튼(`Google.svg`) 적용 + 카카오 버튼 항상 표시
- 범위: `apps/web/src/components/auth/login-form.tsx`
- 할 일 요약: 사용자 직접 구현 지시에 따라 구글 소셜 로그인 버튼을 아이콘+텍스트 스타일로 변경하고, OAuth 환경변수가 없어도 카카오 버튼이 화면에는 보이도록 비활성 표시 처리
- 상태: approved

## 모바일 서가 화면 import 경로 오류 수정

- 제목: `library_screen.dart` 누락 import 경로 정정
- 범위: `apps/mobile/lib/src/ui/screens/library_screen.dart`
- 할 일 요약: 사용자 직접 구현 지시에 따라 `BookDetailScreen`, `LibraryAnalysisScreen` import를 실제 파일 위치(`screens/library/`)로 수정해 컴파일 오류 해결
- 상태: done
