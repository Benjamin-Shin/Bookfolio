# 2026-03-28

**직접 지시(채팅):** `0027_user_book_register_bulk_backfill.sql` — 소장 권수 × `user_book_register` 규칙 점수를 사용자당 원장 1행으로 백필(기존 동일 `reason` 합계 차감).

**직접 지시(채팅):** 관리자 `/dashboard/admin/users` — 브라우저 확장이 주입하는 `data-sharkid` 등으로 SSR/클라이언트 속성 불일치 시 하이드레이션 경고 발생. `<select>`에 `suppressHydrationWarning` 적용, `.cursor/rules/web.mdc`에 하이드레이션·확장 대응 규칙 추가. **상태:** `done`.

**직접 지시(채팅):** 랜딩 `apps/web/src/app/page.tsx` — 포인트·혜택 정책 요약 패널(비회원·회원 안내, 이용약관 링크). **상태:** `done`.

**직접 지시(채팅):** 랜딩 `page.tsx` — 「왜 Bookfolio인가」 이후 최근 기능 소개 패널(모바일 집계·허브·프로필·테마·포인트 등, 웹 집계 안내). **상태:** `done`.

**직접 지시(채팅):** 랜딩 `page.tsx` — iOS 앱은 안드로이드 이후 진행 양해 패널; 개발자·스택 노출 문구 제거·순화. **상태:** `done`.

**직접 지시(채팅):** 랜딩 `page.tsx` — 「왜 Bookfolio인가」+ 구현 목록 통합, 「앞으로 추가될 기능들」계획 카드 분리. **상태:** `done`.

**직접 지시(채팅):** Google Books API 미도입 — 조회 로직·API·UI 문구·약관·개인정보·계획·`books.api_source` 주석(`0028`)·스키마 문서 정리. **상태:** `done`.

**직접 지시(채팅):** Android 런처 표시명 `bookfolio_mobile` → `Seogadam`(`res/values/strings.xml`·`AndroidManifest`). **상태:** `done`.

**설계 근거:** [`docs/bookfolio-points-themes-i18n-vip-design.md`](../bookfolio-points-themes-i18n-vip-design.md)

**구현 진행 (2026-03-28):** 모바일 **로그인 화면** UI 정돈(그라데이션·로고·카드 폼), 관리자 사용자 표 **ID 복사** 버튼, 마이그레이션 `0024`~`0025`, 포인트 API·관리자 규칙 음수·출석, **공동서가** 추가 생성 시 포인트 차감·VIP/`caps.shared_libraries_owned_max`, **멤버 초대** 무료 1명 후 차감·VIP 멤버 상한, **관리자** 규칙별 포인트 적용 폼, **모바일** 서가 서랍→프로필(`GET /api/me/points/balance`), **관리자** VIP 구독 수동 부여(`vip-subscription-action.ts`), **모바일** 화면 모드(시스템/라이트/다크)·팔레트(웜/세이지) 저장(`ThemeController`, `BookfolioThemes`). **남음:** 모바일 i18n(ARB), 유료 테마·포인트 연동, **PG 결제 + VIP 자동 연계**(구독 DB 동기화는 결제 모듈 도입 후 — 설계 문서 §6·`project-plan.mdc`), 운영 cap 카운터, 프로필 `iana_timezone` API 등.

통합 설계(포인트 음수·VIP·플랜 cap·테마·i18n·공동서가·출석·UTC/로컬 시각)를 구현하기 위한 작업 목록이다. 세부 결정은 승인 후 진행한다.

---

## A. 데이터베이스 · 스키마

### A1. `point_rules.points` 음수 허용

- **범위:** `supabase/migrations/`, `docs/bookfolio-db-schema.md`
- **요약:** `CHECK (points >= 0)` 제거(또는 대체). 차감 규칙 저장 가능하도록 반영.
- **상태:** `draft`

### A2. VIP · 플랜 · 구독 테이블

- **범위:** `supabase/migrations/`, `docs/bookfolio-db-schema.md`
- **요약:** `subscription_plans`(`plan_key`, `caps_json`, …), `user_subscriptions`(`plan_key`, `status`, `current_period_end`, 선택 `caps_snapshot_json`). MVP는 관리자 수동 부여 가능 수준.
- **상태:** `draft`

### A3. 출석

- **범위:** `supabase/migrations/`, `docs/bookfolio-db-schema.md`
- **요약:** `user_daily_check_ins`(가칭): `user_id`, 로컬 기준 `check_date`(date), `first_activity_at`(timestamptz UTC), 유니크 `(user_id, check_date)`.
- **상태:** `draft`

### A4. 프로필 타임존 (출석 로컬 일자)

- **범위:** `app_profiles` 또는 `app_users` 확장, 마이그레이션, 스키마 문서
- **요약:** `iana_timezone`(가칭) 선택 컬럼. 미설정 시 UTC 날짜 폴백 정책 문서화.
- **상태:** `draft`

### A5. 운영 cap 카운터 (선택)

- **범위:** `supabase/migrations/` 또는 Redis 연동 설계
- **요약:** `usage_counters` 또는 TTL 캐시로 4.1 `*_per_day` 소비량 집계. 출석 테이블과 분리.
- **상태:** `draft`

---

## B. 웹 — 포인트 로직 · API

### B1. `tryAwardPointsForEvent` 일반화

- **범위:** `apps/web/src/lib/points/award-points.ts`, 관련 호출부
- **요약:** `delta = rule.points` (음수 허용), `balance_after = prev + delta`, 잔액 < 0 거부. 일/월 한도는 적립·차감 정책(`cap_scope` 등) 결정 후 반영.
- **상태:** `draft`

### B2. 비구독 전용 포인트 차감 / VIP 우회

- **범위:** `apps/web/src/lib/points/`, 신규 `lib/subscription/` 또는 `lib/entitlements/` (가칭)
- **요약:** 공통 헬퍼 `hasActiveVip`, `withinCaps`, `resolveActivePlan`. 기능별 진입에서 VIP면 차감 생략, cap만 검사.
- **상태:** `draft`

### B3. 사용자 포인트 API (모바일·웹 클라이언트)

- **범위:** `apps/web/src/app/api/...` (GET/POST만)
- **요약:** 잔액 조회 GET; 차감/이벤트 적용 POST (`eventCode`, `idempotencyKey`, 선택 `iana_timezone` 또는 로컬 날짜). 인증 세션·Bearer 일관성 검토.
- **상태:** `draft`

### B4. 출석 처리 서비스

- **범위:** 웹 서비스 레이어, 독서 이벤트·`user_books` 생성 경로
- **요약:** 자격 행위 시 로컬 `check_date` 계산 후 insert 또는 skip; 행위별 `awardPoints*`는 기존대로 호출. 선택 `daily_activity_attendance` 보너스 멱등.
- **상태:** `draft`

---

## C. 웹 — 관리자 UI

### C1. 포인트 규칙 편집 음수 허용

- **범위:** `apps/web/src/app/dashboard/admin/points/admin-point-rules-editor.client.tsx`, `actions.ts`
- **요약:** 입력 `min` 제거 또는 음수 허용, 파서·검증 갱신, 도움말 “음수 = 차감”.
- **상태:** `draft`

### C2. 사용자 관리 — 잔액 · 지급 · VIP 부여

- **범위:** `apps/web/src/app/dashboard/admin/users/page.tsx`, 신규 폼·actions
- **요약:** 잔액 컬럼(서브쿼리·뷰·RPC로 N+1 방지). 규칙 지급(이벤트·멱등·메모), 선택 직접 조정(`admin_adjustment`). 선택 VIP/구독 기간 수동 부여.
- **상태:** `draft`

### C3. 포인트 원장 필터 (선택)

- **범위:** `apps/web/.../admin/points/page.tsx`
- **요약:** 사용자별 필터·딥링크로 운영 편의.
- **상태:** `draft`

---

## D. 공동서가 · 초대

### D1. 생성 게이트 — VIP / cap / 포인트

- **범위:** `apps/web/src/lib/libraries/repository.ts` (`createLibrary`), 정책 헬퍼
- **요약:** VIP: `caps.shared_libraries_owned_max` 등만 검사. 비구독: 기존 `sharedLibraryCreateLimit` + 추가 슬롯 시 음수 이벤트 차감 후 생성. UI 메시지·에러 코드 정합.
- **상태:** `draft`

### D2. 초대(멤버 추가) 게이트

- **범위:** 공동서가 멤버·초대 API/액션(기존 초대 플로우)
- **요약:** 서가당 무료 1명(소유자 제외 카운트 규칙 명문화). 비구독 추가 초대 시 포인트 차감 이벤트. VIP는 cap만. 차감 시점(발송 vs 수락) 정책 결정.
- **상태:** `draft`

---

## E. 모바일 (Flutter)

### E1. API 연동 — 잔액 · 구독 · 차감

- **범위:** `apps/mobile/lib/src/services/`, `auth_controller` 등
- **요약:** 프로필/설정 화면용 잔액·VIP 표시. 테마 잠금 해제 등 POST apply-event. 타임존/로컬 날짜 전달 정책.
- **상태:** **draft**

### E2. 테마 레지스트리 · `ThemeController`

- **범위:** `apps/mobile/lib/src/app.dart`, 신규 `theme/` 또는 `state/theme_controller.dart`
- **요약:** `AppThemeRegistry`, 기본 light/dark, 유료 테마는 서버 반영 잠금.VIP 시 프리미엄 조건.
- **상태:** `draft`

### E3. 프로필 / 설정 / 테마 상점 화면

- **범위:** `apps/mobile/lib/src/ui/screens/`, `library_screen.dart` drawer
- **요약:** 신규 진입점, 포인트·구독·테마 구매 UX.
- **상태:** `draft`

### E4. i18n 인프라 + 점진적 문자열 이전

- **범위:** `apps/mobile/pubspec.yaml`, `lib/l10n/*.arb`, 화면 다수
- **요약:** `flutter_localizations`, `gen-l10n`, drawer·앱바 우선 순위 치환.
- **상태:** `draft`

### E5. 테마 색 하드코딩 축소 (점진)

- **범위:** 각 `*_screen.dart`
- **요약:** `Theme.of(context)` 기반으로 이전해 신규 테마와 일치.
- **상태:** `draft`

---

## F. 공유 패키지 · 이벤트 코드

### F1. `POINT_EVENT_CODES` 등 확장

- **범위:** `apps/web/src/lib/points/event-codes.ts`, `packages/shared` (필요 시)
- **요약:** 테마 잠금, 공동서가 추가, 초대 추가, 출석 보너스 등 코드 상수·한글 설명 동기화.
- **상태:** `draft`

---

## G. 결제 (후속)

### G1. PG 연동

- **범위:** 별도 스펙
- **요약:** VIP 자동 갱신, 웹훅, `user_subscriptions` 동기화. 본 번들의 C2 수동 부여 이후 단계로 두어도 됨.
- **상태:** `draft`

---

## 메모

- **2026-03-28:** 사용자 직접 지시 — `apps/web/src/app/privacy/page.tsx` 개인정보처리방침을 현재 기능 기준으로 정비함 (승인: 본 지시).
- **2026-03-28:** 사용자 직접 지시 — `apps/web/src/app/terms/page.tsx` 서비스 약관을 현재·예정 기능 기준으로 정비함 (승인: 본 지시).
- **2026-03-28:** 사용자 직접 지시 — `apps/web/src/app/cookies/page.tsx` 쿠키 정책을 현재 구현(NextAuth 등) 기준으로 정비함 (승인: 본 지시).
- **2026-03-28:** 사용자 직접 지시 — **북폴리오 집계** 페이지(웹·모바일): 회원 TOP10(소장 건수·완독·포인트·다중 소장 도서), 헤더 순서 (공동서가 → 북폴리오 집계 → 베스트셀러 → 초이스 신간 → 내 서가), 모바일 허브 동일 (승인: 본 지시).
- **승인:** 본 문서 항목은 채팅·검토 후 `approved`로 바꾼 뒤 구현한다 (`.cursor/rules/workflow-todo-approval.mdc`).
- **의존 순서:** A1 → B1 → C1 이후 Feature API(B3); A2·B2는 공동서가(D)·모바일 차감과 병행 가능. 출석(A3·B4)은 타임존(A4)과 함께 검토하면 안전.
