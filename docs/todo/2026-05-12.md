# 2026-05-12

## 대시보드 통합 도서 검색 (아이콘 → 좌측 입력, user_books → books → 알라딘)

- **범위:** `apps/web/src/app/dashboard/page.tsx`, 신규 API·클라이언트 컴포넌트, `lib/aladin`
- **할 일:** 검색 아이콘으로 좌측 패널 열기, 입력 시 3단 소스 검색 및 각각 상세 URL 연결 (`/dashboard/books/:id`, `/discovery/books/by-isbn/...`, 알라딘 상품 링크)
- **상태:** `approved` — 사용자가 본 턴에서 구체적 구현을 직접 지시함

## API 리다이렉트 — `0.0.0.0` 호스트

- **범위:** `apps/web/src/lib/http/redirect-url.ts`, `app/api/me/books/route.ts`, `app/api/me/books/[id]/route.ts`
- **할 일:** `next dev -H 0.0.0.0` 등에서 폼 POST 후 `Location: http://0.0.0.0:...` 로 인한 연결 오류 방지
- **상태:** `done` — 사용자 보고에 따른 즉시 수정

## 모바일 — 쉘 헤더·하단 유지, 발견 브레드크럼, 드로어 메뉴 정리

- **범위:** `apps/mobile/lib` (`MainShellScreen`, 드로어, 프로필·통계·베스트·초이스 신간 진입)
- **할 일:** (1) 프로필·내 서가 통계·베스트·초이스 신간 푸시 시 상단 쉘·하단 탭 유지 (`embeddedInShell`) (2) 베스트·초이스에 `발견 > …` 브레드크럼 (3) 드로어에서 「통계·서가담 집계」 제거
- **상태:** `done` — 구현 완료

## 모바일 — 프로필도 쉘 헤더·하단 유지

- **범위:** `main_shell_v2_screen.dart`, `bookfolio_navigation_drawer.dart`
- **할 일:** `body` 중첩 [Navigator]에서 [ProfileScreen] `push` — 루트 스택이 아닌 쉘 본문 스택
- **상태:** `done` — 사용자 직접 지시 반영

## 모바일 — 브레드크럼 「발견」 탭으로 복귀

- **범위:** `discovery_flow_breadcrumb.dart`, `main_shell_tab_scope.dart`, `main_shell_v2_screen.dart`
- **할 일:** 베스트·초이스 브레드크럼에서 「발견」 탭 시 발견 탭으로 이동
- **상태:** `done`

## 모바일 — 하단 탭 전환 시 본문 스택 정리

- **범위:** `main_shell_v2_screen.dart` (`_goTab`)
- **할 일:** 베스트/초이스 등 `body` [Navigator] 위에 쌓인 화면이 있을 때 다른 탭 클릭 시 `popUntil` 후 탭 전환
- **상태:** `done`

## 모바일 — 드로어에서 내 서가 통계 등 쉘 본문 푸시

- **범위:** `main_shell_v2_screen.dart`, `bookfolio_navigation_drawer.dart`, `home_screen.dart`
- **할 일:** 햄버거에서 내 서가 통계·베스트·초이스가 루트 `Navigator`를 타던 문제 → `open*InShell` 콜백으로 본문 스택 푸시
- **상태:** `done`

## 모바일 발견 — 분야별 탐색 카테고리 갱신

- **범위:** `discover_screen.dart`, `main_shell_v2_screen.dart`, `bookfolio_navigation_drawer.dart`
- **할 일:** 프로필 저장 후 `IndexedStack` 발견 탭이 옛 프로필로 남는 문제 → `refreshSignal`·탭 전환·프로필 복귀 시 `_load()`; 동일 CID 다행 시 가장 깊은 depth 행 선택
- **상태:** `done` — 구현 완료

## 모바일 — 프로필 일부 메뉴 비노출

- **범위:** `apps/mobile/lib/src/ui/screens/etc/profile_screen.dart`
- **할 일:** 해금 가능한 기능·포인트 관리(내역·획득·해금)·계정/알림 설정 UI 주석 처리
- **상태:** `done` — 사용자 직접 지시 반영

## 모바일 — 프로필 편집 화면

- **범위:** `apps/mobile/lib/src/ui/screens/etc/profile_edit_screen.dart`
- **할 일:** (1) 계정 관리(탈퇴) 블록 비노출 (2) 다른 폼 화면과 맞춘 타이포·카드·여백·로딩 UX
- **상태:** `done` — 구현 완료

## 모바일·웹 — 햄버거에서 쿠키 정책 제거

- **범위:** `bookfolio_navigation_drawer.dart`, `site-header-mobile-nav.client.tsx`, `site-header.tsx`, `site-footer.tsx`, `next.config.mjs`, `apps/web/src/app/cookies/page.tsx` 삭제, `bookfolio_web_urls.dart` 주석
- **할 일:** Flutter 드로어·웹 모바일 Sheet·푸터에서 「쿠키 정책」·`/cookies` 제거; `/cookies` → `/privacy` 리다이렉트
- **상태:** `done` — 사용자 직접 지시 반영
