# TODO — 2026-03-24

## 항목

### 1. 관리자 사용자 목록 — 이름 인라인 수정

- **범위**: `apps/web/src/app/dashboard/admin/users/page.tsx`, `user-name-form.tsx`(신규), `apps/web/src/app/dashboard/admin/actions.ts`
- **요약**: 사용자 표시명을 테이블에서 입력·저장할 수 있게 함 (`app_profiles`·`app_users` 동기화는 `upsertAppProfileRow` 활용)
- **상태**: `approved`
- **승인**: 사용자가 채팅에서 본 기능 구현을 직접 요청함 (2026-03-24)

### 2. 모바일 책 등록 — 제목 검색

- **범위**: `apps/web/src/lib/books/lookup.ts`, `apps/web/src/app/api/books/search-by-title/route.ts`(신규), `apps/mobile/lib/src/services/bookfolio_api.dart`, `library_controller.dart`, `book_form_screen.dart`, `title_search_screen.dart`(신규)
- **요약**: ISBN 외에 제목으로 외부 메타 검색(네이버·Google 폴백) 후 결과 목록에서 선택해 폼에 채움
- **상태**: `done`
- **승인**: 사용자가 채팅에서 제목 검색 기능 추가를 직접 요청함 (2026-03-24)

### 3. `books` — 옮긴이·API소스 컬럼 및 관리자 편집

- **범위**: `supabase/migrations/0013_books_translators_api_source.sql`(신규), `apps/web/src/app/dashboard/admin/books/[id]/edit/page.tsx`, `admin-canonical-book-form.tsx`, `actions.ts`, `lib/books/repository.ts`(`DbCanonicalBook` 타입)
- **요약**: `translators`(text[]), `api_source`(text, nullable) 추가; 관리 도서 수정·신규 폼에서 입력·저장; ISBN 조회 시 API소스 칸은 비어 있을 때만 조회 `source`로 채움
- **상태**: `done`
- **승인**: 사용자가 채팅에서 컬럼 추가 및 관리 페이지 편집을 직접 요청함 (2026-03-24)

### 5. 로그인 후 대시보드 콜백 이동

- **범위**: `apps/web/src/components/auth/login-form.tsx`
- **요약**: Credentials 로그인 직후 `router.push` 대신 전체 네비게이션으로 이동해 미들웨어가 세션 쿠키를 확실히 읽도록 함
- **상태**: `done`
- **승인**: 사용자가 로그인 후 콜백 이동 실패 문제 해결을 직접 요청함 (2026-03-24)

### 12. 대시보드 `PAGE_SIZE`를 `BOOKS_PER_SHELF`와 재동기

- **범위**: `apps/web/src/app/dashboard/page.tsx`
- **요약**: `PAGE_SIZE`가 12로 남아 3줄(18권)이 안 나오던 문제 — `PAGE_SIZE = BOOKS_PER_SHELF`로 복구
- **상태**: `done`
- **승인**: 사용자 증상 보고 (2026-03-24)

### 11. 책장 시각적 행 수 상수(`VISUAL_ROWS_PER_SHELF`)

- **범위**: `apps/web/src/components/books/bookshelf.tsx`, `apps/web/src/app/dashboard/page.tsx`
- **요약**: `VISUAL_ROWS_PER_SHELF`·`BOOKS_PER_SHELF = BOOKS_PER_VISUAL_ROW * …` export, 대시보드 `PAGE_SIZE` 동기
- **상태**: `done`
- **승인**: 사용자 직접 요청 (2026-03-24)

### 10. 책장 행 가운데 정렬

- **범위**: `apps/web/src/components/books/bookshelf.tsx`
- **요약**: 책 행 `justify-center`·`w-full`
- **상태**: `done`
- **승인**: 사용자 직접 요청 (2026-03-24)

### 9. 책장 첫 줄 세로 스크롤바 제거

- **범위**: `apps/web/src/components/books/bookshelf.tsx`
- **요약**: `overflow-x-auto`+`overflow-y-visible` 제거, 행마다 `pb-10`·`overflow-y-hidden`으로 세로 스크롤 방지
- **상태**: `done`
- **승인**: 사용자 직접 요청 (2026-03-24)

### 8. 책장 한 줄 6권·선반 12권

- **범위**: `apps/web/src/components/books/bookshelf.tsx`, `apps/web/src/app/dashboard/page.tsx`(주석 동기)
- **요약**: 시각적 행당 6권; 선반 덩어리는 `PAGE_SIZE`(12)와 일치
- **상태**: `done`
- **승인**: 사용자가 채팅에서 직접 지정 (2026-03-24)

### 7. 대시보드 — 소장 페이지네이션·선반 행별 받침줄

- **범위**: `apps/web/src/app/dashboard/page.tsx`, `dashboard-books-toolbar.tsx`, `components/books/bookshelf.tsx`, (선택) `lib/books/repository.ts` RPC 파싱 보강
- **요약**: 소장 총권수 기준 페이지네이션이 보이도록 보정·배치; 읽는 중/소장 선반에서 줄바꿈 행마다 받침(선반 앞판) 표시
- **상태**: `done`
- **승인**: 사용자가 채팅에서 해당 UI 수정을 직접 요청함 (2026-03-24)

### 6. ISBN·제목 조회 우선순위 (books → 네이버 → 국립중앙도서관)

- **범위**: `apps/web/src/lib/books/lookup.ts`, `apps/web/src/lib/books/repository.ts`, `apps/web/src/app/api/books/lookup-by-isbn/route.ts`, `apps/web/src/app/api/books/search-by-title/route.ts`
- **요약**: ISBN 외부 조회는 네이버 → 국립중앙도서관 → Google 순으로 시도(로컬 `books`는 기존처럼 최우선). 제목 검색은 `books` 제목 부분일치 → 네이버 → 국립중앙도서관 `SearchApi.do`의 `title` 파라미터, 결과는 앞선 출처 우선으로 ISBN 기준 중복 제거
- **상태**: `approved`
- **승인**: 사용자가 채팅에서 우선순위를 직접 지정함 (2026-03-24)

### 13. 관리자 ISBN 검색 — 네이버·국립중앙도서관 버튼 분리

- **범위**: `apps/web/src/lib/books/lookup.ts`, `apps/web/src/app/api/books/lookup-by-isbn/route.ts`, `apps/web/src/app/dashboard/admin/books/admin-canonical-book-form.tsx`
- **요약**: 관리자 공유 서지 폼에서 ISBN 조회를 **국립중앙도서관 전용**·**네이버 전용** 버튼으로 분리; `POST /api/books/lookup-by-isbn`에 `provider`(naver|nl|google) 선택 시 해당 API만 호출하고 로컬 `books` 우선 반환은 생략
- **상태**: `done`
- **승인**: 사용자가 채팅에서 네이버·국립 API 검색 분리를 직접 요청함 (2026-03-24)

### 4. 저자 마스터 테이블·도서 연결·관리 페이지

- **범위**: `supabase/migrations/0014_authors_book_authors.sql`, `lib/books/replace-book-author-links.ts`, `lib/books/repository.ts`, `admin/books/actions.ts`, `dashboard/admin/authors/*`, `admin/layout.tsx`, `admin/page.tsx`
- **요약**: `authors`·`book_authors` 추가, 백필 및 트리거로 `books.authors` 동기화, RPC `replace_book_author_links`, 관리자 저자 목록·신규·이름 수정
- **상태**: `done`
- **승인**: 사용자가 채팅에서 저자 관리 페이지·테이블·books 관계 일괄 추가를 직접 요청함 (2026-03-24)

### 14. 저자 관리 — 연결 도서 0명 일괄 삭제

- **범위**: `apps/web/src/app/dashboard/admin/authors/actions.ts`, `delete-zero-book-authors-button.tsx`(신규), `page.tsx`
- **요약**: `book_authors`에 한 번도 안 나오는 저자 마스터 행을 관리자가 한 번에 삭제
- **상태**: `done`
- **승인**: 사용자가 채팅에서 일괄 삭제 기능 추가를 직접 요청함 (2026-03-24)
