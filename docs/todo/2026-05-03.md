# TODO 2026-05-03

## Stitch 어두운 로그인 시안 — 좌측 패널만 반영

- **범위:** `apps/web/src/components/auth/login-form.tsx`
- **할 일:** Stitch 화면「서가담 로그인 페이지 (어두운 배경)」에서 좌측 이미지 영역·텍스트만 가져와 기존 우측 폼은 유지
- **상태:** `done` (Stitch 프로젝트「Concept Landing Page」화면 기준 반영)

## 로그인 좌측 배경 — Stitch 생성 이미지 URL

- **범위:** `apps/web/src/components/auth/login-form.tsx`, `apps/web/next.config.mjs`
- **할 일:** 좌측 패널 배경을 시안 HTML의 `lh3.googleusercontent.com` 이미지로 사용
- **상태:** `done` (사용자 직접 지시)

---

## 내 서가 대시보드 — Stitch「Concept Landing Page」시안 재구성

- **프로젝트명:** Concept Landing Page (Stitch)
- **범위:** `apps/web/src/app/dashboard/page.tsx`, 신규·분리 컴포넌트는 `apps/web/src/components/dashboard/` 또는 `apps/web/src/components/books/` (팀 일관성에 맞춤), 필요 시 `dashboard-href.ts` 등 쿼리 파라미 확장
- **참고 시안 (내용만 반영, 검색 화면은 상단 글로벌 헤더 제외):**
  1. 서가담 — 내 서가 (한글화·기록 중심 톤·히어로/소개 카피)
  2. 서가담 — 내 서가 (검색·필터링 — 툴바·필터·목록 밀도)
  3. 서가담 — 내 서가 (빈 상태 — CTA·안내)
- **할 일 요약:**
  - 현재 사이드 컬렉션·탭·툴바·페이지네이션·추천·캘린더 로직은 유지하되, 레이아웃·타이포·여백·카피를 시안 1·2·3에 맞게 정리
  - **목록 우측 상단:** 책장(선반+책등) 뷰 ↔ 커버 그리드 뷰 전환을 **별도 클라이언트 컴포넌트**로 분리; 선호는 **`localStorage`** 저장
  - **적용 범위:** 이후 **전 탭**에 표지/책등·톤 통일 적용됨(2026-05-03)
  - **책등 두께:** `readingTotalPages ?? pageCount` 기준 스케일; 값 없을 때는 **최소·최대 두께(px) 상수**로 클램프(구현 시 합리적 기본값 선택)
  - **책등 색:** 커버 색 추출 없이 **`genre_slugs` → 5~7색 팔레트**를 **코드 상수**로 매핑; 미매칭 슬러그는 중립색
- **상태:** `approved` (2026-05-03: 아래 확정 결정 반영; 전 탭 표지/책등·톤 통일; 2026-05-03 좌측 사이드 제거·가로 탭·책등 단일 선반 가로 페이지)

### 확정 결정 (2026-05-03, 채팅)

| 주제 | 확정 |
|------|------|
| 뷰 전환 저장 | `localStorage` |
| 뷰 적용 범위 | 전 탭(이후 구현 반영) |
| 쪽수 없음 | 최소·최대 두께 **상수**로 처리 |
| 책등 색 | 장르 5~7색, **상수 매핑** |

### 참고: 스타일 규칙 (`web.mdc`와의 관계)

- **의미:** “합의”는 팀/문서 차원에서 **인라인 `style`을 어디까지 허용할지**를 정하자는 뜻이었다. `web.mdc`는 Tailwind 유틸 위주를 권장하고, 책등처럼 **권마다 다른 px 폭·HEX 배경**은 클래스만으로는 부담이 크다.
- **처리:** 책등 **전용**으로 `width` / `backgroundColor` 등 **최소한의 `style`** 사용을 허용하고, 컴포넌트 `@history` 또는 PR에 한 줄로 남긴다. (일반 레이아웃·타이포는 기존처럼 Tailwind 유지.)

### 잔여 (구현 시 처리)

- **시안 자산:** Stitch 산출물 중 필요한 이미지는 `public/assets` 로컬화 규칙 준수.
- **장르 슬러그 ↔ 색:** DB에 실제로 오는 `genre_slugs` 값을 보며 상수 테이블을 채운다; 없는 슬러그는 중립색.

---

## 대시보드·전역 헤더 — 참고 이미지(딥 그린·상단 카드·독서 통계)

- **범위:** `apps/web/src/app/dashboard/page.tsx`, `apps/web/src/components/layout/site-header.tsx`, `apps/web/src/components/layout/site-header-mobile-nav.client.tsx`, `apps/web/src/components/layout/header-account.tsx`, `apps/web/src/components/dashboard/*` 신규
- **할 일:** 배경 `#F8F9FA`, 악센트 `#1A3C2F`, 프로필 요약 카드·현재 읽는 책·월간 막대 통계·헤더(내 서가·발견·모임서가·통계·검색·알림·프로필)
- **상태:** `done` (2026-05-03 반영)

---

## 표지 책장 UI — 참고 스니펫(호버 오버레이·얇은 목선반)

- **범위:** `apps/web/src/components/dashboard/dashboard-editorial-book-grid.tsx`
- **할 일:** `EditorialGrid`를 행·`aspect-[2/3]` 셀·호버 시 제목·저자(·읽는 중 진행) 오버레이·`#c9a36a`–`#8b5e3c` 얇은 선반으로 정리
- **상태:** `done` (사용자 참고 스니펫 반영)

---

## 표지 책장 — 선반 3층 구조(그라데이션·암선·블러)

- **범위:** `apps/web/src/components/dashboard/dashboard-editorial-book-grid.tsx`
- **할 일:** 책 행 `flex`·`gap-[6px]`·셀 `w-[90px] aspect-[2/3] mb-[-4px]`·선반 `8px`/`6px`/`6px blur` 층으로 교체
- **상태:** `done` (사용자 직접 마크업 지시)

---

## 표지 책장 — 책 뒤 연한 목재톤 니치

- **범위:** `apps/web/src/components/dashboard/dashboard-editorial-book-grid.tsx`
- **할 일:** 각 선반 행 뒤 `z-0` 그라데이션·얕은 inset 그림자·얇은 링
- **상태:** `done` (2026-05-03)

---

## 표지 책장 — `rounded-xl` 카드·음영·책 그림자·선반 10px×3

- **범위:** `apps/web/src/components/dashboard/dashboard-editorial-book-grid.tsx`
- **할 일:** 사용자 제공 마크업으로 `EditorialGrid` 행 래퍼 전면 교체
- **상태:** `done` (2026-05-03)

---

## 표지 책장 — 권별 기울기·접촉 그림자·선반 하이라인

- **범위:** `apps/web/src/components/dashboard/dashboard-editorial-book-grid.tsx`
- **할 일:** `indexInRow` 기반 `translate`/`rotate`·책 하단 접촉 그림자·선반 12/2/10/12px·행 전체 그라데이션 그림자 제거
- **상태:** `done` (2026-05-03)

---

## 웹 뷰포트 규칙·선반 복귀·스프라이트 안내

- **범위:** `.cursor/rules/web.mdc`, `dashboard-editorial-book-grid.tsx`
- **할 일:** 웹은 데스크톱·최소 태블릿 기준·모바일은 앱; 선반 `10×3` 복귀; 소품 스프라이트 맵 안내 문단
- **상태:** `done` (2026-05-03)

---

## 책장 소품 스프라이트 스켈레톤

- **범위:** `lib/dashboard/shelf-prop-sprites.ts`, `components/dashboard/shelf-prop-sprite.tsx`, `dashboard-editorial-book-grid.tsx`, `web.mdc`
- **할 일:** 맵·스타일 헬퍼·시드 선택·`EditorialGrid` 양쪽 슬롯·`NEXT_PUBLIC_SHELF_PROPS_SPRITE_ENABLED`
- **상태:** `done` (2026-05-03)

---

## 책장 데코 인라인 혼합(`mixItems` 참고)

- **범위:** `shelf-row-mix.ts`, `shelf-decor-inline.tsx`, `shelf-prop-sprites.ts`, `dashboard-editorial-book-grid.tsx`, `web.mdc`
- **할 일:** 시드 기반 18% 삽입·`DecorKind`→스프라이트·`EditorialShelf` 12/2/10/12·`shelfPropHash32` export
- **상태:** `done` (2026-05-03)

---

## 책장 데코 기본 표시(비표시 원인: `ENABLED=1` 미설정)

- **범위:** `dashboard-editorial-book-grid.tsx`, `shelf-decor-inline.tsx`, `web.mdc`
- **할 일:** 기본 ON·`SHELF_PROPS_SPRITE_DISABLED=1`로만 끔·데코 슬롯 배경
- **상태:** `done` (2026-05-03)

---

## 책장 데코 회색 박스(shelf-props.png 없음) → SVG `<img>`

- **범위:** `shelf-row-mix.ts`, `shelf-decor-inline.tsx`, `public/assets/shelf-decor/*.svg`, `web.mdc`
- **할 일:** `DECOR_KIND_IMAGE_URL` + `<img>`로 표시, 플레이스홀더 SVG 4종 추가
- **상태:** `done` (2026-05-03)

---

## Editorial 선반 라인 박스형 원복

- **범위:** `dashboard-editorial-book-grid.tsx`
- **할 일:** `EditorialShelf`를 `shelfRowLedgeMid`/`shelfLedge` 사용, 행 카드 `rounded-lg`
- **상태:** `superseded` — 사용자 스니펫(4층 목·`rounded-xl`)이 의도였음; 동 파일에서 재정렬

---

## Editorial 선반 — 참고 스니펫(4층·rounded-xl)으로 정렬

- **범위:** `dashboard-editorial-book-grid.tsx`
- **할 일:** `EditorialShelf` 12/2/10/12 blur·행 `rounded-xl`
- **상태:** `done` (2026-05-03)

---

## Editorial 책장 — 참고 UI(밝은 벽·목 선반·브래킷·그림자)

- **범위:** `dashboard-editorial-book-grid.tsx`, `shelf-decor-inline.tsx`
- **할 일:** 행 배경 단색·벽 느낌 음영·목재 두께 선반·코벨·선반 하단 그림자
- **상태:** `done` (2026-05-03: 사용자 직접 지시)
