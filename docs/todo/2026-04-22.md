# 2026-04-22 TODO

- 제목: 모바일 로그인 헤더 로고 교체
  - 범위(경로/모듈): `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`, `apps/mobile/pubspec.yaml`
  - 할 일 요약: 로그인 페이지 상단의 "서가담" 텍스트를 `assets/brand/Seogadam_Web_logo.png` 이미지로 교체하고 에셋 등록 반영
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 관리자 도서 목록 필터/정렬 개선
  - 범위(경로/모듈): `apps/web/src/app/dashboard/admin/books/page.tsx`
  - 할 일 요약: 검색 필터에 저자 검색을 포함하고 목록 정렬 우선순위에 `page_count` 비어있는 항목 우선 정렬을 추가
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 관리자 도서 목록 정렬 선택 추가
  - 범위(경로/모듈): `apps/web/src/app/dashboard/admin/books/page.tsx`
  - 할 일 요약: 정렬 기준을 UI에서 선택할 수 있도록 `sort` 쿼리 파라미터 및 선택 셀렉트 추가
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 모바일 소셜 로그인 에셋 경로 교체
  - 범위(경로/모듈): `apps/mobile/lib/src/ui/screens/sign_in_screen.dart`, `apps/mobile/pubspec.yaml`
  - 할 일 요약: 카카오/구글 로그인 이미지 경로를 `apps/mobile/assets` 기준으로 교체하고 구글 버튼을 카카오와 동일 슬롯 크기에 맞춤
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 웹 프로필 다이얼로그 2열 레이아웃 확장
  - 범위(경로/모듈): `apps/web/src/components/layout/header-account.tsx`
  - 할 일 요약: 프로필 다이얼로그를 2열로 재배치해 아바타 업로드를 우측 열로 이동하고 다이얼로그 폭을 약 1.5배로 확장
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 웹 프로필 다이얼로그 좌측 열 1/3 비율 조정
  - 범위(경로/모듈): `apps/web/src/components/layout/header-account.tsx`
  - 할 일 요약: 2열 그리드에서 좌측 1열 너비를 전체 대비 1/3(우측 2/3)로 고정
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 프로필 이미지 최종 URL 입력 숨김
  - 범위(경로/모듈): `apps/web/src/components/layout/profile-avatar-upload-field.tsx`
  - 할 일 요약: 프로필 편집에서 `프로필에 쓸 이미지 주소` 입력 필드를 숨기고 업로드 중심 UX로 단순화
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 웹 헤더 관리자 아이콘을 설정 아이콘으로 변경
  - 범위(경로/모듈): `apps/web/src/components/layout/site-header.tsx`
  - 할 일 요약: 프로필 좌측 관리자 설정 아이콘을 방패 아이콘에서 설정(톱니) 아이콘으로 교체
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 알라딘 목록 API 파라미터 조합형 리팩터링 + 카테고리 필터
  - 범위(경로/모듈): `apps/web/src/lib/aladin/*`, `apps/web/src/app/dashboard/bestsellers/page.tsx`, `apps/web/src/app/dashboard/choice-new/page.tsx`, `apps/web/src/app/api/me/aladin-*/route.ts`, `apps/mobile/lib/src/services/bookfolio_api.dart`, `apps/mobile/lib/src/ui/screens/*`
  - 할 일 요약: `ALADIN_API_BASE_URL`에 URL/키까지만 두고 QueryType·CategoryId 등은 유틸에서 조합, CSV를 3Depth까지 상수화해 웹/모바일에서 카테고리별 조회 적용
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 알라딘 카테고리 4단계 연쇄 선택 UI
  - 범위(경로/모듈): `apps/web/src/app/dashboard/bestsellers/page.tsx`, `apps/web/src/app/dashboard/choice-new/page.tsx`, `apps/mobile/lib/src/ui/screens/bestseller_screen.dart`, `apps/mobile/lib/src/ui/screens/choice_new_screen.dart`
  - 할 일 요약: 카테고리 선택을 단일 드롭다운에서 4단계(몰/1Depth/2Depth/3Depth) 연쇄 선택으로 변경하고 각 셀렉트에 라벨 없이 `카테고리` 플레이스홀더를 적용
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 베스트셀러/초이스 카테고리 필터 hydration 경고 완화
  - 범위(경로/모듈): `apps/web/src/app/dashboard/bestsellers/page.tsx`, `apps/web/src/app/dashboard/choice-new/page.tsx`
  - 할 일 요약: 확장 프로그램 주입 속성(`data-sharkid`)으로 인한 SSR/CSR 속성 불일치 경고를 `suppressHydrationWarning`으로 완화
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 웹 카테고리 연쇄 선택 즉시 반영
  - 범위(경로/모듈): `apps/web/src/components/dashboard/aladin-category-cascade-filter.tsx`, `apps/web/src/app/dashboard/bestsellers/page.tsx`, `apps/web/src/app/dashboard/choice-new/page.tsx`
  - 할 일 요약: 적용 버튼 방식 대신 카테고리 선택 즉시 쿼리 파라미터를 갱신해 다음 단계 목록이 즉시 보이도록 변경
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 알라딘 목록 가격 표시를 정가로 변경
  - 범위(경로/모듈): `apps/web/src/components/dashboard/aladin-ttb-item-list-view.tsx`, `apps/mobile/lib/src/ui/screens/bestseller_screen.dart`, `apps/mobile/lib/src/ui/screens/choice_new_screen.dart`
  - 할 일 요약: 베스트셀러/초이스신간 목록 카드에서 판매가(`priceSales`) 대신 정가(`priceStandard`)를 표시
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 책 추가 API의 알라딘 가격 반영을 정가 우선으로 변경
  - 범위(경로/모듈): `apps/web/src/lib/aladin/admin-book-prefill.ts`, `apps/web/src/app/dashboard/admin/books/actions.ts` (및 관련 추가 API 경로)
  - 할 일 요약: 모바일/웹에서 책 추가 시 알라딘 가격 매핑이 판매가(`priceSales`) 우선이 아닌 정가(`priceStandard`) 우선으로 들어가도록 변경
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 정가 미존재 시 판매가 폴백 일관화
  - 범위(경로/모듈): `apps/web/src/components/dashboard/aladin-ttb-item-list-view.tsx`, `apps/mobile/lib/src/ui/screens/bestseller_screen.dart`, `apps/mobile/lib/src/ui/screens/choice_new_screen.dart`
  - 할 일 요약: 가격 표시를 정가 우선으로 유지하되 정가가 없으면 기존 판매가(`priceSales`)로 자동 폴백
  - 상태: `approved` (사용자 구현 지시로 승인 간주)

- 제목: 추천 시스템 1·2차 DB/로직 구현 + 3차 계획 문서화
  - 범위(경로/모듈): `supabase/migrations/*`, `docs/bookfolio-db-schema.md`, `docs/*plan*`, `apps/web/src/*`
  - 할 일 요약: 1차(상호작용/노출 로그), 2차(사용자 선호 프로필/도서 피처 벡터)를 Supabase에 추가하고 관련 서버 로직을 구현, 3차 항목은 운영 고도화 계획으로 문서에만 반영
  - 상태: `done` (사용자 구현 지시로 승인 간주)

- 제목: 내 서가 추천 패널 추가
  - 범위(경로/모듈): `apps/web/src/app/dashboard/page.tsx`, `apps/web/src/components/books/*`, `apps/web/src/lib/recommendations/*`
  - 할 일 요약: 내 서가 화면에 추천 API(`GET /api/me/recommendations`)를 연동한 추천 패널을 추가해 추천 도서를 노출
  - 상태: `done` (사용자 구현 지시로 승인 간주)

- 제목: 모바일 홈 추천 패널 API 연동
  - 범위(경로/모듈): `apps/mobile/lib/src/ui/screens/home_screen.dart`, `apps/mobile/lib/src/services/bookfolio_api.dart`
  - 할 일 요약: 모바일 홈의 추천 섹션을 추천 API(`GET /api/me/recommendations`) 기반으로 교체하고 상호작용 로깅을 연결
  - 상태: `done` (사용자 구현 지시로 승인 간주)
